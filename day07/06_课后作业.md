# Day 7 课后作业 · 周测订正与 Week 1 总结

> 今日以周测为主；以下作业面向 **未满分学员订正** 与 **全体 Week 1 复盘**。请在周日 21:00 前提交。

## 必做 B1 · 周测错题订正

1. 将笔试错题抄入错题本，每题注明：
   - 错误原因（概念混淆 / 粗心 / 未复习）
   - 正确知识点所在 Day 与讲义章节
   - 一句话总结
2. 机试未通过的 AC 项，对照 PRD 补做并附运行截图或终端录屏说明

## 必做 B2 · 通讯录手撕流程

不运行程序，在纸上画出：

1. `add` 从输入到 `append` 的流程图（≥5 步）  
2. `quit` 时数据如何落盘到 `contacts.json`  
3. 重启后 `next_id` 从哪里恢复  

## 必做 B3 · JSON 文件阅读

打开 `code/contacts.json`，回答：

1. 第三条联系人的 `department` 是什么？  
2. `next_id` 为 6 表示什么？  
3. 写出 Python 表达式读取 `contacts[0].email`（假设已 `load` 到变量 `data`）

## 必做 B4 · 扩展 search（二选一）

在 `address_book.py` 或 `homework/address_book_ext.py` 中实现：

**方案 A**：搜索结果显示匹配字段高亮（用 `【】` 包裹子串即可，不用 colorama）  

**方案 B**：增加 `export` 命令，将当前 `contacts` 导出为 `contacts_backup_YYYYMMDD.json`

## 必做 B5 · Week 1 学习总结

写 `homework/week1_summary.md`（300～500 字），包含：

1. 本周最难的三个点  
2. 你最有成就感的一次运行/调试  
3. 对 Day 8 OOP 的期待或顾虑  

## 必做 B6 · Git Week 1 标签

```bash
git add homework/day07/
git commit -m "homework(day07): 姓名 - week1 总结与订正"
git tag week1-姓名   # 可选：方便讲师检索
git push
```

## 选做 X1 · 导入 Day 5 员工数据

写脚本 `import_employees.py`：读取 `day05/code/mock_api_response.json` 中的 `employees`，转换字段映射后 **合并** 进 `contacts.json`（跳过重复手机号）。

字段映射建议：

| Mock 字段 | 通讯录字段 |
|-----------|------------|
| name | name |
| phone | phone |
| department | department |
| employee_id | notes 前缀 |

## 选做 X2 · 命令行参数

用 `sys.argv` 支持：

```bash
python3 address_book.py --file my_contacts.json
```

未传参时仍用默认 `contacts.json`。

## 挑战 C1 · 无 JSON 文件启动

实现：首次启动自动创建含一条示例联系人的 `contacts.json`，并打印欢迎说明。

## 挑战 C2 · 为 Day 8 画类图

阅读 `address_book.py`，在作业中手绘或 Mermaid 画出你心目中的 `Contact` 与 `ContactBook` 类图（属性 + 方法列表）。

---

提交路径：`homework/day07/你的姓名/`

讲师复查：周一早会抽查 3 人现场演示 `search` + `save` + 重启验证。
