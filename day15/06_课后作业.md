# Day 15 课后作业

> 提交目录：`homework/day15/`  
> Git commit message 建议：`feat(day15): Token 计量与成本估算作业`

---

## 必做 A · tiktoken 实操（30 分）

### A1. 中英对比表

在 `homework/day15/token_compare.py` 中：

1. 准备 3 组「语义相近」的中英句子  
2. 用 `tiktoken.encoding_for_model("gpt-4o-mini")` 统计 Token  
3. 打印表格：句子 | 字符数 | Token 数 | Token/字符比  

验收：运行脚本无报错，中文组 Token/字符 比通常 ≥ 英文组。

### A2. 工单样例计数

读取 `homework/day15/sample_ticket.txt`（自建，≥ 200 字），输出：

- 纯文本 Token 数  
- 加上系统提示「你是工单助手」后的 messages 总 Token（用课程近似公式）

---

## 必做 B · 成本估算（35 分）

### B1. 扩展场景

复制 `code/token_cost_estimator.py` 逻辑到 `homework/day15/my_cost_estimate.py`，新增参数：

```bash
python3 my_cost_estimate.py --rag-chunks 5 --chunk-tokens 400
```

含义：RAG 额外塞入 5 段，每段 400 Token，累加到输入。

### B2. 立项书一页纸

在 `homework/day15/cost_summary.md` 用 **非技术人员能看懂** 的语言写：

1. 假设条件（日均次数、输出长度）  
2. 推荐模型及月费区间（低/中/高）  
3. 一句风险说明（「实际以 API usage 为准」）

---

## 必做 C · 模型测验（20 分）

1. 完成 `code/model_landscape_quiz.md` 全部题目  
2. 在 `homework/day15/quiz_answers.md` 写答案与错题订正  

---

## 必做 D · 客户端衔接（15 分）

在 `homework/day15/llm_usage_check.py`：

```python
from llm_compat import get_llm_client, CLIENT_SOURCE

client = get_llm_client()
# mock 或 live 调用一次 chat，打印 prompt_tokens
```

`notes.md` 说明 `CLIENT_SOURCE` 指向 Day12 还是 Day13。

---

## 选做 E · 多模型词表对比

安装 `transformers`，用不同 tokenizer 对比同一段中文的 Token 数，写 5 行结论。

---

## 选做 F · 汇率与人民币报价

在成本 JSON 中增加 `monthly_cost_cny`（假设汇率 7.25），更新 `cost_summary.md`。

---

## 挑战 G · 历史对话膨胀模拟

写脚本模拟 10 轮对话，每轮用户 50 Token、助手 150 Token，绘制「累计输入 Token」折线（可用 print 表格代替画图）。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `token_compare.py` | A |
| `sample_ticket.txt` | A |
| `my_cost_estimate.py` | B |
| `cost_summary.md` | B |
| `quiz_answers.md` | C |
| `llm_usage_check.py` + `notes.md` | D |

---

**截止时间**：Day 16 上午 09:00 前 push 到个人分支。
