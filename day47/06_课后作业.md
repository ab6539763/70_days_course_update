# Day 47 课后作业

## 必做

### B1 · 新分析问题

在 `MockTextToSQL.generate_sql` 增加对「待处理订单有哪些」以外的第 5 个启发式规则（如「硬件品类销量」），并更新 verify 测试。

### B2 · SQL 手写

不运行 Agent，手写一条查询「华东区域订单总金额」的 SQL，附在 `homework/day47/manual.sql`。

### B3 · 验收截图

`verify_day47.py` 全绿。

## 选做

### O1 · 接入 InputGuard

修改 `text_to_sql_agent.py`，在 `ask()` 前调用 Day 46 `InputGuard`。

### O2 · Live 模式

有 Key 时调用真实 LLM 生成 SQL，保留 `validate_sql` 不变。

## 挑战

### C1 · 结果可视化

用 `tabulate` 或纯文本表格美化 `summarize` 输出。

---

*答案见 [07_作业参考答案.md](./07_作业参考答案.md)*
