# Day 47 架构与设计 · Text-to-SQL 流水线

## 1. 组件图

```text
┌──────────┐   ┌─────────────┐   ┌──────────────┐   ┌────────┐
│ 用户问题 │──▶│ MockTextToSQL│──▶│ validate_sql │──▶│ SQLite │
└──────────┘   └─────────────┘   └──────────────┘   └───┬────┘
                                                          │
                        ┌─────────────┐                   │
                        │  summarize  │◀──────────────────┘
                        └──────┬──────┘
                               ▼
                          自然语言答案
```

## 2. Schema 注入策略

教学版：将 `SCHEMA_SUMMARY` 文本放入 Prompt（live 模式）。  
Mock 版：启发式规则直接映射问题 → SQL，不调用 API。

## 3. 安全层

```python
# 必须通过
sql.upper().startswith("SELECT")
not FORBIDDEN_SQL.search(sql)
";" not in sql.rstrip(";")  # 单语句
```

## 4. ER 关系

```text
customers 1───* orders 1───* order_items *───1 products
```

## 5. 与 Day 48 衔接

Text-to-SQL 可作为办公 Agent 的 `query_sales_db` 工具，外包给本日 `TextToSQLAgent.ask()`。

---

*流程图见 [05_流程图与示意图.md](./05_流程图与示意图.md)*
