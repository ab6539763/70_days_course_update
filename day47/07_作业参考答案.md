# Day 47 作业参考答案

## B2 · 华东区域订单总金额 SQL

```sql
SELECT SUM(oi.line_amount) AS east_china_total
FROM order_items oi
JOIN orders o ON oi.order_id = o.order_id
WHERE o.region = '华东';
```

## B1 · 硬件品类销量示例

```python
if "硬件" in question:
    return (
        "SELECT p.name, SUM(oi.quantity) AS qty "
        "FROM order_items oi JOIN products p ON oi.product_id = p.product_id "
        "WHERE p.category = 'Hardware' "
        "GROUP BY p.name;"
    )
```

## O1 · InputGuard 集成示例

```python
from agent_guardrails import AgentGuardrails

guard = AgentGuardrails()

def ask(self, question: str) -> SQLResult:
    inp = guard.validate_input(question)
    if not inp.allowed:
        raise ValueError("输入未通过护栏")
    ...
```

## C1 · tabulate 示例

```python
from tabulate import tabulate
return tabulate(rows, headers="keys", tablefmt="github")
```

---

*主课：[04_课堂讲义.md](./04_课堂讲义.md)*
