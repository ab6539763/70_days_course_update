# Day 26 课后作业

> 提交目录：`homework/day26/`

---

## 必做 A · 验收（25 分）

`python3 code/verify_day26.py` 全绿截图 → `verify_ok.png`

---

## 必做 B · 意图分类链（35 分）

仿 [Day 18 intent_classifier](../day18/code/intent_classifier.py)，在 `homework/day26/intent_lcel_chain.py` 实现：

```python
chain = prompt | llm | JsonOutputParser(pydantic_object=IntentResult)
```

字段：`intent`, `confidence`, `reason`。mock 模式可运行。

---

## 必做 C · batch 翻译（40 分）

扩展 `translation_chain.py` 思路，在 `homework/day26/batch_translate.py`：

1. 读取至少 5 条中文 SKU（可手写 list）  
2. 使用 `chain.batch` 译英  
3. 输出 CSV：`source,translation`

---

## 选做 D · stream（10 分）

对 `build_simple_lcel_chain()` 调用 `chain.stream`，打印逐 token/chunk。

---

## 提交清单

- [ ] `verify_ok.png`  
- [ ] `intent_lcel_chain.py`  
- [ ] `batch_translate.py` + 输出 CSV
