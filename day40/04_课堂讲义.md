# Day 40 课堂讲义 · LangChain Agent

## 第一章 · @tool（09:00–10:30）

```python
from langchain_core.tools import tool

@tool
def classify_ticket(text: str) -> str:
    """对工单描述进行意图分类。"""
    ...
```

运行：`python3 lc_tools.py`

**要点**：函数名 = 工具名；docstring = 描述；类型注解 → 参数 schema。

## 第二章 · create_tool_calling_agent（10:30–12:00）

```python
from langchain.agents import create_tool_calling_agent

prompt = ChatPromptTemplate.from_messages([
    ("system", SYSTEM_PROMPT),
    ("human", "{input}"),
    MessagesPlaceholder("agent_scratchpad"),
])
agent = create_tool_calling_agent(llm, tools, prompt)
```

## 第三章 · AgentExecutor（14:00–15:30）

```python
executor = AgentExecutor(
    agent=agent,
    tools=tools,
    verbose=True,
    max_iterations=6,
    return_intermediate_steps=True,
)
result = executor.invoke({"input": "VIP 退款加急"})
```

对比 [Day 39 react_agent.py](../day39/code/react_agent.py) 的 `for step in range(max_steps)`。

## 第四章 · search_calc_agent（15:30–17:00）

精简三工具演示多场景切换：`search_kb` + `calculator` + `classify_ticket`。

```bash
python3 verify_day40.py
```

## 小结

明日 Day 41：同一业务用 **LangGraph** 画成图，加 **approval 人工节点**。
