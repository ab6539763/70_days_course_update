# Day 40 业务背景 · LangChain Agent 接管工单路由

## 1. 事件单

**事件单号**：PHASE3-LC-AGENT-2026-0805  
**关联**：[Day 39 ReAct](../day39/)、[Day 19 FC](../day19/)、[Day 36 RAG](../day36/)

张工评审 Day 39 代码：

> 「循环写得对，但生产不能全靠 regex 解析 ReAct 文本。Day 40 用 **LangChain Tool Calling Agent**——`@tool` 生成 schema，`AgentExecutor` 管循环。」

### 业务目标

| 能力 | Day 39 | Day 40 |
|------|--------|--------|
| 工具定义 | 手写 TOOL_REGISTRY | `@tool` 装饰器 |
| 模型输出 | 文本 Action | 结构化 tool_calls |
| 循环 | 手写 while | AgentExecutor |
| 可观测 | ReActTrace | intermediate_steps |

## 2. 今日任务

实现 `lc_agent.py`：输入工单描述 → 自动 classify → route → 中文汇总。

知识检索 `search_kb` 概念对接 [Day 36 project2](../day36/code/project2/backend/rag_service.py)。

## 3. 学习目标

1. 掌握 `@tool` 与 docstring → JSON schema  
2. 使用 `create_tool_calling_agent` + `ChatPromptTemplate`  
3. 理解 `AgentExecutor` 的 max_iterations、verbose  
4. mock 模式无 Key 验收  

---

*Day 40 · Phase3 第二课*
