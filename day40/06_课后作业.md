# Day 40 课后作业

## 必做

**B1** 在 `lc_tools.py` 增加 `@tool get_ticket(ticket_id: str)`，读取 Day 39 mock_tickets.json。

**B2** 为 `lc_agent.py` 增加 `stream` 模式演示（`executor.stream`），打印每步 event。

**B3** `verify_day40.py` 增加 get_ticket 测试。

## 选做

**X1** live 模式接真实 OpenAI，对比 mock 与 live 的 intermediate_steps。

## 挑战

**C1** 用 `StructuredTool.from_function` 替代 `@tool`，写对比笔记。

---

commit 含 `day40`
