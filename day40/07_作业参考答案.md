# Day 40 作业参考答案

## B1 · get_ticket

```python
@tool
def get_ticket(ticket_id: str) -> str:
    """按工单号查询详情。"""
    from tools_basic import get_ticket_by_id
    return json.dumps(get_ticket_by_id(ticket_id), ensure_ascii=False)
```

## B2 · stream

```python
for chunk in executor.stream({"input": query}):
    print(chunk)
```

## 讲评

AgentExecutor 的 scratchpad 等价于 Day 19 messages 里的 assistant+tool 往返；理解这一点就不怕框架「黑盒」。

---

*Day 40 参考答案*
