# Day 43 课后作业

## 必做
1. 跑通 `supervisor_multi_agent.py` + verify 全绿  
2. 阅读 `agent_roles.py`，为 Writer 增加「字数上限 500」的 system_prompt  
3. 画一张 Supervisor 循环图（可手绘拍照或 Mermaid）  

## 选做
4. 新增 `Reviewer` 角色：Writer 完成后 Reviewer 打分，<80 分回到 Writer  
5. 将 Searcher 改为调用 Day 33 `hybrid_retriever`（mock 语料即可）  

## 挑战
6. Supervisor 路由改为 LLM 决策（mock 下用 FakeListChatModel 返回 agent 名）
