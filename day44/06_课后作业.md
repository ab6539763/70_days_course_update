# Day 44 课后作业

## 必做
1. 跑通三个脚本 + verify 全绿  
2. 阅读 `mcp_compat.py`，说明 `MCP_AVAILABLE=False` 时如何降级  
3. 给 `agent_with_mcp.py` 增加一条测试问题，触发双工具调用  

## 选做
4. 新增 MCP 工具 `employee_lookup(name)` 返回 mock 组织架构  
5. 用官方 MCP stdio Client 连接 `simple_mcp_server.py --stdio`  

## 挑战
6. 将 Day 43 Searcher 改为通过 MCP `kb_search` 获取资料
