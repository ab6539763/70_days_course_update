# Day 44 作业参考答案

## Mock 降级

`mcp_compat.MCP_AVAILABLE=False` 时：
- `simple_mcp_server.demo_list_tools()` 走 `MockMCPServer`  
- `demo_call_tool` 同样路由到 mock 实现  
- verify 中 `test_mock_fallback` 专门覆盖此路径

## 双工具问题示例

```
工单 INC-2026-100 进展？退款政策是什么？
```

`plan_tools` 应返回 `ticket_status` + `kb_search`。

## employee_lookup（选做）

```python
@mcp.tool()
def employee_lookup(name: str) -> str:
    return json.dumps({"name": name, "dept": "研发", "lead": "张工"})
```

## Searcher + MCP（挑战）

在 `agent_roles.SEARCHER.run` 内：

```python
from mcp_client_demo import SparkTechMCPClient
raw = SparkTechMCPClient().call("kb_search", query=task)
```
