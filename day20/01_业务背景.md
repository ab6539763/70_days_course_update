# Day 20 业务背景 · FAQ 重复问句与多模态质检需求

## 1. 事件回顾

**事件单号**：REQ-FAQ-EMBED-2026-0920  
**发起人**：客服主管 + 质检组  
**关联**：Day 19 Function Calling、Day 22 RAG  
**严重程度**：P1（影响客服效率，不阻塞发版）  
**截止日期**：今日 17:30 提交相似问匹配原型 + 多模态 demo

### 1.1 现场情况

星火智服 FAQ 库 2000+ 条，用户问法千奇百怪：

| 现象 | 数据 | 业务损失 |
|------|------|----------|
| 同义不同问 | 「咋退款」「如何退钱」「能退吗」 | 重复建单，人工重复回答 |
| 关键词匹配失效 | 「到账慢」匹配不到「退款多久到账」 | 误转人工 |
| 质检靠人眼 | 产线缺陷需老师傅看图 | 多模态模型可辅助 |

张工在需求评审：

> 「Day 20 两件事：**Embedding 相似问匹配** + **Vision API 预习**。  
> 向量用 cosine 排序；无 Key 用 mock 向量，但流程与生产一致。  
> 相似问匹配器明天可封装成 Day 19 的 Agent 工具。」

### 1.2 与 Day 19 的对比

| 维度 | Day 19 工具调用 | Day 20 向量检索 |
|------|-----------------|-----------------|
| 核心 | 模型决定调函数 | 向量空间语义相似 |
| 数据 | 实时 API / 计算 | 预索引 FAQ |
| 输出 | 工具 JSON 结果 | Top-K 相似问 + 分数 |
| 扩展 | 加 schema | 加向量库（Day 21） |

## 2. 今日你在项目中的任务

```text
  上午  Embedding 原理 + embedding_demo
        │
        ▼
  上午  cosine_similarity NumPy 实现
        │
        ▼
  下午  similar_question_matcher 索引 + 检索
        │
        ▼
  下午  multimodal_demo 图像理解
        │
        ▼
  Day 21  向量数据库（Chroma / FAISS）
```

## 3. 与大模型开发的隐性联系

| 今日知识点 | 日后场景 |
|------------|----------|
| text-embedding | RAG 召回、FAQ 去重、聚类 |
| cosine similarity | 向量检索标准度量 |
| 阈值 threshold | 命中置信度、转人工策略 |
| Vision API | 票据 OCR、质检、图文问答 |
| mock 向量 | 离线测试、CI 流水线 |

## 4. 今日学习目标（业务语言）

1. 理解 Embedding：把文本映射到向量空间  
2. 掌握余弦相似度计算与几何意义  
3. 实现 FAQ 相似问匹配器（索引 + Top-K）  
4. 了解多模态 API 基本调用方式  
5. mock 模式完成端到端演示  

## 5. 验收口径

| 检查项 | 通过标准 |
|--------|----------|
| cosine | 相同向量相似度 = 1.0 |
| embedding | 相同文本 mock 向量一致 |
| matcher | 「如何申请退款」命中退款 FAQ，score > 0.5 |
| multimodal | mock 返回图片描述 |
| verify | 9/9 通过 |

---

**讲师备注**：今日是 **RAG 的向量层基础**，Day 22 将串联完整检索增强生成链路。
