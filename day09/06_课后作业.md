# Day 9 课后作业

> 提交目录：`homework/day09/`  
> Git commit message 建议：`feat(day09): 继承作业 + DeepSeek 扩展`

---

## 必做 A · 继承与多态（40 分）

### A1. 实现 `DeepSeekModel`

在 `homework/day09/deepseek_model.py` 中：

```python
from base_model import BaseModel, GenerationResult

class DeepSeekModel(BaseModel):
    vendor = "deepseek"
    # 默认 model_name = "deepseek-chat"
    # 实现 generate() mock，口吻区别于 OpenAI 与 Qwen
```

要求：

- `super().__init__(...)` 正确调用  
- `generate` 开头调用 `super()._before_generate(prompt)`  
- 返回 `GenerationResult`，`vendor="deepseek"`  

### A2. 多态路由函数

在 `homework/day09/router.py` 中实现：

```python
def first_response(models: list, prompt: str) -> str:
    """依次尝试 models 中的每个模型，返回第一个 generate 的 text。"""
```

参数类型注解应为 `list[BaseModel]`。禁止在函数内写 `if vendor == ...`。

### A3. 口述题（写在 `homework/day09/README.md`）

1. 继承与「把一个 BaseModel 实例放进 list」有什么关系？  
2. 为什么业务代码推荐依赖 `BaseModel` 而不是 `OpenAIModel`？  

---

## 必做 B · 魔术方法与 property（30 分）

### B1. 补全 `ChatMessage` 类

在 `homework/day09/chat_message.py` 中补全：

```python
class ChatMessage:
    def __init__(self, role: str, content: str):
        self._role = role
        self._content = content

    @property
    def role(self) -> str: ...

    @property
    def content(self) -> str: ...

    @content.setter
    def content(self, value: str) -> None:
        # 空字符串抛 ValueError

    def __str__(self) -> str:
        # 格式：[user] 你好

    def __repr__(self) -> str:
        # ChatMessage(role='user', content='你好')
```

### B2. `__call__` 实验

写 5 行代码证明：`callable(OpenAIModel())` 为 `True`，且 `m("测")` 使 `call_count` 变为 1。

### B3. `__str__` vs `__repr__`

各写一句：**何时**在日志里用 `str(model)`，**何时**用 `repr(model)`？

---

## 必做 C · 综合（30 分）

### C1. 扩展注册表

修改或复制 `run_llm_demo.py` 为 `homework/day09/run_my_llm_demo.py`：

- 注册至少 1 个 `DeepSeekModel`  
- 对同一 prompt 打印三家厂商各一条结果  
- 退出前打印所有模型的 `call_count`  

### C2. 画类图

在 `homework/day09/class_diagram.md` 用 Mermaid 或 ASCII 画出今日完整类层次（含 `GenerationResult`）。

---

## 选做 D · Mixin 日志（+10 分）

参考 `magic_methods_demo.VerboseOpenAI`，为 `QwenModel` 写 `LoggingQwenModel`，在 `generate` 前后 `print` 日志，且 **必须** 使用 `super().generate(...)`。

---

## 挑战 E · 简易模型工厂（+15 分）

```python
def create_model(vendor: str, **kwargs) -> BaseModel:
    """vendor 支持 openai / qwen / deepseek，未知 vendor 抛 ValueError。"""
```

要求用 **多态** 返回正确子类，函数内禁止三段重复构造逻辑以外的 `if` 链（允许 `if vendor == "openai": return OpenAIModel(**kwargs)` 这种 **工厂分支**，但业务 `generate` 路由仍不得 if 链）。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `deepseek_model.py` | A1 |
| `router.py` | A2 |
| `README.md` | A3, B3 |
| `chat_message.py` | B1 |
| `run_my_llm_demo.py` | C1 |
| `class_diagram.md` | C2 |

---

**截止时间**：Day 10 09:00 课前提交 MR。
