# Day 49 课堂讲义 · LangGraph 与全栈联调

> **讲师**：张工 · 预计 3.5 小时

---

## 第 1 节 · office_graph 精读（50 min）

打开 `graph/office_graph.py`：

1. `StateGraph(OfficeState)` 注册 5 节点  
2. `compile(checkpointer=MemorySaver())`  
3. `human_gate_node` 中 `interrupt()`  

演示 Python REPL：

```python
from graph.office_graph import OfficeGraphService
svc = OfficeGraphService()
s = svc.start_task("起草会议邮件")
s["status"]  # awaiting_approval
svc.resume_task(s["thread_id"], {"decision": "approve"})
```

## 第 2 节 · interrupt / Command（40 min）

```python
from langgraph.types import Command
graph.invoke(Command(resume={"decision": "approve"}), config)
```

**踩坑**：resume 必须带与 interrupt 返回结构一致的 dict。

## 第 3 节 · FastAPI（45 min）

路由对照 `02_需求文档.md`：

- 创建任务后立刻 `GET` 校验 steps 长度 ≥3  
- resume 后 `result.email.status == sent`  

```bash
uvicorn backend.main:app --port 8010
curl http://127.0.0.1:8010/api/health
```

## 第 4 节 · 前端联调（50 min）

1. `bash run.sh`  
2. 打开 `http://127.0.0.1:8088`  
3. 提交任务 → 审批 → 看结果 JSON  

检查 `app.js` 中 `renderState` 对 `pending_approval` 的处理。

## 第 5 节 · verify 与彩排（35 min）

```bash
python3 verify_project3.py
```

逐项对照：

- graph interrupt + resume  
- graph reject  
- API 全流程  

---

## 讲师板书

```
User → POST /tasks → Graph.run → interrupt
User → POST /resume → Command → executor → done
```

---

*Day 49 课堂讲义*
