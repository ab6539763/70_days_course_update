# Day 49 业务背景 · 人工审批与任务恢复

## 1. 合规事件

**事件单号**：COMP-AUDIT-2026-0902  
**来源**：信息安全部  

> 「自动化系统外发邮件必须 **可中断、可审计、可恢复**。演示时要能展示：用户拒绝后流程终止，服务重启后 thread 仍可 resume。」

## 2. Day 49 签收标准

1. LangGraph `human_gate` 节点调用 `interrupt()`  
2. FastAPI 暴露 create / get / resume  
3. 前端审批面板绑定 resume API  
4. `verify_project3.py` 覆盖 approve 与 reject 路径  
5. `run.sh` 默认 mock 端口 8010 / 8088  

## 3. Demo 脚本（5 分钟预习）

1. 输入：「请检索退款政策并起草通知邮件，预约周三产品会议」  
2. 展示 plan 与 researcher steps  
3. 展开审批预览（邮件 subject/body）  
4. 点击 **批准发送** → status=completed  
5. 新任务演示 **拒绝** → status=rejected  

## 4. 与 Day 24 SSE 的差异

| 维度 | Day 24 聊天流 | Day 49 任务流 |
|------|---------------|---------------|
| 协议 | SSE delta | REST + 轮询/刷新 state |
| 暂停点 | 无 | interrupt 审批 |
| 状态 | session messages | thread + checkpointer |

---

*Day 49 · LangGraph 联调日*
