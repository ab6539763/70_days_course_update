# Day 49 作业参考答案

## B3 · Demo 台词卡示例

| 时间 | 台词 |
|------|------|
| 0:00 | 这是 Project 3 多 Agent 办公助手，mock 模式无需 Key |
| 0:30 | 输入复合任务：检索制度、起草邮件、约会议 |
| 1:00 | Planner 拆三步，Researcher 调 RAG 与搜索 |
| 1:30 | Writer 出草稿，流程在 human_gate interrupt |
| 2:00 | 主管审阅 subject/body，点批准 |
| 2:30 | Executor 调 email_send 与 calendar_schedule，status=completed |
| 3:00 | 第二遍演示拒绝，强调合规终止 |

## X1 · SqliteSaver 片段

```python
from langgraph.checkpoint.sqlite import SqliteSaver
import sqlite3
conn = sqlite3.connect("data/checkpoints/office.db", check_same_thread=False)
checkpointer = SqliteSaver(conn)
return graph.compile(checkpointer=checkpointer)
```

## X2 · audit 片段

```python
def log_approval(thread_id, decision, comment):
    with open("data/approval.jsonl", "a") as f:
        f.write(json.dumps({...}) + "\n")
```

---

*Day 49 参考答案*
