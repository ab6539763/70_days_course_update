# Day 49 课后作业

## 必做

### B1 · 完整 verify 截图

运行 `day49/verify_project3.py`，保存 `=== ALL PASSED ===` 终端截图。

### B2 · 录制 3 分钟屏幕录像

包含：提交任务 → 展示 steps → 批准 → 完成；再演示一次拒绝。

### B3 · Demo 台词卡

写 `homework/day49/demo_script.md`（≤300 字），按秒标注讲解点。

## 选做

### X1 · SqliteSaver

将 `MemorySaver` 换为 `langgraph.checkpoint.sqlite.SqliteSaver`，路径 `data/checkpoints/office.db`。  
验证：重启 uvicorn 后仍可 resume 同一 `thread_id`。

### X2 · 审批审计日志

新增 `backend/audit.py`，每次 resume 追加 JSONL 行：`thread_id`, `decision`, `comment`, `ts`。

## 挑战

前端增加「刷新状态」按钮，不重新提交任务即可轮询 `GET /api/tasks/{id}`。

---

**截止**：Day 50 答辩前
