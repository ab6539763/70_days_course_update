# Day 28 课后作业

> 提交目录：`homework/day28/`  
> Git commit message 建议：`feat(day28): Document Loader 与 Splitter 作业`

---

## 必做 A · Loader 扩展（25 分）

### A1. 新增一种格式

在 `homework/day28/` 新增 **JSON** 或 **JSONL** 样本（≥5 条 FAQ），写 `my_json_loader.py`：

- 使用 `JSONLoader` 或自写 `BaseLoader`  
- 每条记录 `page_content` = question + answer  
- `metadata` 含 `id`、`category`

### A2. 加载报告

运行后输出 `loader_report.txt`：格式名、Document 数、总字符数。

---

## 必做 B · Splitter 调参（35 分）

### B1. 自定义语料

合并至少 3 个自有 txt/md 文件（可复用 sample_docs），总字符 ≥ 3000。

### B2. 参数网格

对 `chunk_size` ∈ {200, 350, 500} × `overlap` ∈ {0, 40, 80} 共 9 组跑 Recursive splitter。

输出 CSV：`splitter_grid.csv` 列：

```text
chunk_size,chunk_overlap,num_chunks,avg_len,max_len
```

### B3. 选型说明

`notes.md` 写 200 字：你为星火智服 FAQ 选哪组参数，为什么。

---

## 必做 C · PDF 流水线（30 分）

### C1. 处理自有 PDF

将任意 PDF（或课程样本）放入 `homework/day28/docs/`，修改 `process_my_ebook.py`：

- 支持命令行 `--chunk-size`、`--overlap`  
- 导出 JSON 至 `output/`

### C2. 块质量人工抽检

随机抽 5 个 chunk，标注是否「语义完整」（是/否），写入 `chunk_audit.md`。

---

## 选做 D · 外网 Web Loader

有外网环境时对比 `BSHTMLLoader`（本地）与 `WebBaseLoader`（远程同一 URL）：

- Document 数差异  
- 正文噪声（导航栏、脚本）差异  

---

## 选做 E · metadata  enrich

为所有 chunk 增加 `metadata["department"] = "知识库运营"`，写 `enrich_metadata.py` 演示继承关系。

---

## 挑战 F · 语义切块预览

调研 `MarkdownHeaderTextSplitter`，对 `product_guide.md` 按 `#` / `##` 标题切块，与 Recursive 对比块数。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `my_json_loader.py` + 样本 | A |
| `splitter_grid.csv` + `notes.md` | B |
| `process_my_ebook.py` + JSON | C |
| `chunk_audit.md` | C |

---

**截止时间**：Day 29 上午 09:00 前 push。
