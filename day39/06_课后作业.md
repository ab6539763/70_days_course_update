# Day 39 课后作业

## 必做（提交 Git，commit 含 `day39`）

### B1 · 扩展 classify_ticket

在 `tools_basic.py` 增加 `billing_dispute` 意图，关键词含「对账」「发票不符」。  
验收：`classify_ticket("对账金额不符")` → `billing_dispute`。

### B2 · 增强解析器

`parse_react_output` 支持 `Action Input` 被 ```json 代码块包裹的情况。  
写 2 个单元测试用例在 `verify_day39.py` 或独立 `test_parse.py`。

### B3 · 新场景 demo

在 `react_loop_demo.py` 增加 Part 5：技术类工单「API 500」完整路由 demo。

## 选做

### X1 · 对接 Day 36

写 `search_kb_live(query)` 存根：读取 `day36/code/project2/data/sample_docs/` 做简单关键词检索。

### X2 · 轨迹导出

`ReActTrace.to_json_file(path)` 导出 JSON，供运营分析路由准确率。

## 挑战

### C1 · 多 Action 规划

研究 **Plan-and-Execute**：先让 LLM 输出完整计划列表，再逐步执行。写 200 字对比 ReAct。

---

**提交**：Git push + 课堂扫码登记 commit hash。
