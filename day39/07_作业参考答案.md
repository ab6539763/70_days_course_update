# Day 39 作业参考答案

## B1 · billing_dispute 意图

```python
INTENT_KEYWORDS["billing_dispute"] = ["对账", "发票不符", "金额不符", "账单争议"]
INTENT_ROUTING["billing_dispute"] = {
    "team": "billing",
    "sla_hours": "12",
    "label": "账单争议组",
}
```

## B2 · 代码块 Action Input

```python
RE_JSON_BLOCK = re.compile(r"```(?:json)?\s*(\{.+?\})\s*```", re.DOTALL)

def parse_react_output(text: str) -> dict[str, str]:
    ...
    block = RE_JSON_BLOCK.search(text)
    if block and "action_input" not in result:
        result["action_input"] = block.group(1).strip()
```

## B3 · API 500 demo

```python
def demo_api_ticket() -> None:
    agent = ReActAgent(verbose=True)
    trace = agent.run("生产环境 API 持续 500 错误，工单 TK-20260701-001")
    assert "engineering" in trace.final_answer.lower() or len(trace.steps) >= 2
```

## X1 · Day 36 简易检索

```python
def search_kb_live(query: str, limit: int = 3) -> dict:
    docs_dir = Path(__file__).resolve().parents[2] / "day36/code/project2/data/sample_docs"
    # 遍历 txt/md，关键词匹配...
```

## 讲评要点

1. ReAct 解析要考虑 LLM 输出不稳定——多写几种 regex fallback  
2. 业务意图表要与产品文档同步，避免 mock 与生产漂移  
3. 明日 Day 40 用 `@tool` 后，schema 由框架生成，但循环本质不变  

---

*Day 39 参考答案 v1.0*
