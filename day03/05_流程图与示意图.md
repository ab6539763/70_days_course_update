# Day 3 流程图与示意图

## 1. 主菜单 REPL

```mermaid
flowchart TD
    Start([启动 simple_menu]) --> Loop{while True}
    Loop --> Prompt[显示菜单 + 等待输入]
    Prompt --> Route{输入类型?}
    Route -->|/exit| End([sys.exit 0])
    Route -->|/clear| Clear[clear_screen]
    Route -->|/help| Help[打印帮助]
    Route -->|1| G[guess_number.main]
    Route -->|2| M[multiplication_table.main]
    Route -->|3| F[flow_control_demo.main]
    Route -->|其他| Err[未知命令提示]
    Clear --> Loop
    Help --> Loop
    G --> Loop
    M --> Loop
    F --> Loop
    Err --> Loop
```

## 2. if / elif / else 决策

```mermaid
flowchart TD
    A[读取 guess] --> B{guess 是整数?}
    B -->|否| C[continue 重试]
    B -->|是| D{guess vs secret}
    D -->|小于| E[提示太小]
    D -->|大于| F[提示太大]
    D -->|等于| G[break 猜中]
    E --> H[attempts += 1]
    F --> H
    H --> I{attempts < max?}
    I -->|是| A
    I -->|否| J[公布答案]
    G --> K[结束本局]
    J --> K
```

## 3. for 循环 · 九九表

```text
i = 1 ──▶ j: 1 ──▶ 打印 1×1=1
          │
i = 2 ──▶ j: 1,2 ──▶ 打印 2×1=4  2×2=4
          │
i = 3 ──▶ j: 1,2,3
          │
  ... 直到 i = 9
```

```mermaid
flowchart LR
    subgraph outer["外层 i = 1..9"]
        subgraph inner["内层 j = 1..i"]
            P[print i×j]
        end
    end
    inner --> NL[换行]
    NL --> outer
```

## 4. break vs continue

```text
  for i in range(10):
      if i % 2 == 0:
          continue    ──▶ 跳过本次，i+1 继续
      if i == 7:
          break       ──▶ 整个循环结束
      print(i)

  输出: 1 3 5 7  （7 时 break，不打印 7）
```

| 关键字 | 作用范围 | 典型场景 |
|--------|----------|----------|
| `continue` | 跳过**当前这一次**迭代 | 非法输入不计次 |
| `break` | 跳出**整个**循环 | 猜中、用户 `/exit` |

## 5. range 三种形式

```text
range(5)        → 0, 1, 2, 3, 4
range(2, 6)     → 2, 3, 4, 5
range(0, 10, 2) → 0, 2, 4, 6, 8
```

业务映射：

- `range(1, 10)`：九九表行号  
- `range(max_attempts)`：固定次数尝试（0 起算需注意 +1 显示）  
- `range(len(items))`：Day 4 遍历 list 下标  

## 6. Day 3 → Day 14 命令体系演进

```mermaid
flowchart LR
    D3["Day3 /exit /clear /help"]
    D6["Day6 函数化 handler"]
    D14["Day14 + 对话历史 messages"]
    D20["Day20 + /save /load JSON"]
    D3 --> D6 --> D14 --> D20
```

```text
  Day 3  simple_menu:
           if raw == "/exit": break

  Day 14 chat_assistant:
           if raw == "/clear":
               messages.clear()
           elif raw == "/exit":
               break
           else:
               messages.append({"role":"user","content":raw})
               reply = call_api(messages)
```

## 7. 第一周学习链（更新）

```mermaid
flowchart LR
    D1[变量 IO] --> D2[字符串清洗]
    D2 --> D3[分支循环 CLI]
    D3 --> D4[list 待办]
    D4 --> D5[dict JSON]
    D5 --> D6[函数重构]
    D6 --> D7[通讯录周测]
```

## 8. 嵌套条件示意 · 成绩 + 补考

```text
           score
             │
             ▼
        score >= 60 ?
        /          \
      是            否
      │              │
  grade A/B/C    makeup >= 60 ?
  (嵌套 if)      /            \
               是              否
            补考及格          不及格
```

## 9. while 空输入重试（修复 Day 1）

```mermaid
flowchart TD
    Start([开始]) --> Read[input 姓名]
    Read --> Empty{strip 后为空?}
    Empty -->|是| Warn[提示不能为空]
    Warn --> Read
    Empty -->|否| Ok[继续后续字段]
```

---

*示意图可直接用于集训 PPT 与答辩材料。*
