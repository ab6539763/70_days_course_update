# Day 3 课后作业

## 必做 B1 · if/elif 成绩等级

完成 `homework/grade_hw.py`：

- 输入整数成绩 `score`（0–100）  
- 输出等级：A≥90，B≥80，C≥60，D<60  
- 非法输入（非数字、越界）提示并重输（`while`）

## 必做 B2 · while 空输入

写 `homework/read_nonempty.py`，实现函数：

```python
def read_nonempty(prompt: str) -> str:
    """循环直到非空，返回 strip 后的字符串。"""
```

用其读取姓名并打印问候语。

## 必做 B3 · 九九表指定范围

在课堂 `multiplication_table.py` 基础上增加选项 3：

- 输入 `a,b`（1≤a≤b≤9），只打印第 `a` 到 `b` 行  
- 用双重 `for` 或调用 `print_row` 循环  

## 必做 B4 · 猜数字增强

复制为 `homework/guess_number_hw.py`：

- 增加「难度」：简单 1–50，困难 1–200（`if/elif` 选择）  
- 记录每局猜测列表（预习 list：`guesses = []`，`append`）  
- 猜中后打印「你共猜了 N 次，记录： [...]」

## 必做 B5 · 菜单命令

在 `simple_menu.py` 增加 `/history`（预习）：打印「今日暂无对话历史，Day 14 实现」（占位字符串即可）。

## 必做 B6 · LeetCode Easy × 2

1. **LC 412 FizzBuzz**：本地 `homework/fizzbuzz.py`，`n=20` 运行截图或粘贴输出  
2. **LC 1 Two Sum**：`homework/two_sum_brute.py`，暴力双重循环版即可  

README 写一句：暴力法时间复杂度 O(n²)，Day 5 用 dict 优化。

## 选做 X1

猜数字支持「自定义 max_attempts」，输入 0 时使用默认值 7。

## 选做 X2

`simple_menu.py` 用 `os.system("cls" if os.name == "nt" else "clear")` 替代 ANSI 清屏，兼容 Windows。

## 挑战 C1

不用 `break`，仅用 `while condition` 条件变量实现猜数字（练习布尔标志 `won = False`）。

## 挑战 C2

打印**菱形**图案（空格+星号），行数 `n` 由用户输入，嵌套 `for` + `if`。

```text
  *
 ***
*****
 ***
  *
```
（n=3 示例）

---

提交：`homework/day03/你的姓名/`，commit `homework(day03): 姓名`
