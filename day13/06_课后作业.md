# Day 13 课后作业

> 提交目录：`homework/day13/`  
> Git commit message 建议：`feat(day13): 装饰器与弹性客户端作业`

---

## 必做 A · 装饰器（30 分）

### A1. 缓存装饰器

在 `homework/day13/memo_decorator.py` 实现：

```python
def memoize(func):
    """无参装饰器：相同参数第二次调用直接返回缓存。"""
```

对 `expensive_add(a, b)` 演示：同参数第二次不打印「计算中」。

### A2. 带参重试

在 `homework/day13/retry_lab.py` 中：

1. `from api_decorators import retry`（或复制课程实现）  
2. 实现 `unstable_divide(a, b)`：前 2 次 `b==0` 时抛 `ZeroDivisionError`，第 3 次正常返回  
3. 用 `@retry(max_attempts=4, delay=0.1, exceptions=(ZeroDivisionError,))` 装饰  

验收：运行脚本输出最终商。

---

## 必做 B · 生成器（25 分）

### B1. 日志行生成器

在 `homework/day13/log_reader.py` 实现：

```python
def iter_errors(path: str) -> Generator[str, None, None]:
    """逐行读取，只 yield 含 ERROR 的行。"""
```

用课程 `data/sample.log`（自建即可）演示，**不得** `readlines()` 一次读入。

### B2. 批量切片

实现 `batched(iterable, size)` 生成器，每次 yield 最多 `size` 个元素的 list。  
示例：`list(batched(range(7), 3))` → `[[0,1,2], [3,4,5], [6]]`

---

## 必做 C · 弹性客户端（35 分）

### C1. 扩展 ResilientLLMClient

在 `homework/day13/my_resilient_client.py`：

```python
from resilient_llm_client import ResilientLLMClient

class MyResilientClient(ResilientLLMClient):
    def summarize_ticket(self, title: str, body: str) -> str:
        """构造 messages 调用 chat，返回 text。"""
```

`main()` 演示 mock 模式总结一条假工单。

### C2. .env 配置

1. 复制 `code/.env.example` 为 `homework/day13/.env.example`  
2. 本地 `.env` 不入库  
3. 在 `notes.md` 说明无 Key 时如何跑通 mock  

### C3. 验收截图

运行 `python3 verify_day13.py`（或作业目录等价脚本），截图含 `[OK] mock chat`。

---

## 选做 D · typing 强化

为 `summarize_ticket` 的参数定义 `TypedDict`：

```python
class Ticket(TypedDict):
    id: str
    title: str
    body: str
```

重写方法签名为接收 `Ticket`。

---

## 选做 E · asyncio 并发

在 `homework/day13/async_batch.py`：

- 用 `asyncio.gather` 并发调用 5 次 `call_llm_async`（可复制 `asyncio_intro.py`）  
- 打印总耗时，证明小于串行  

---

## 选做 F · tenacity 对比

```bash
pip install tenacity
```

在 `notes.md` 用 5 行以内对比手写 `@retry` 与 `tenacity.retry` 的优缺点。

---

## 挑战 G · 按状态码重试

扩展 `api_decorators` 或作业客户端：

- 仅当 `LLMHTTPError.status_code >= 500` 时重试  
- 429 读取 `Retry-After` 头（mock 即可）  

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `memo_decorator.py` | A |
| `retry_lab.py` | A |
| `log_reader.py` | B |
| `my_resilient_client.py` | C |
| `notes.md` | C |
| `verify 截图` | C |
| `async_batch.py` | E 选做 |
| `Ticket TypedDict` | D 选做 |

---

**截止时间**：Day 14 上午 09:00 前 push 到个人分支。
