# Day 23 课后作业

> 提交目录：`homework/day23/`  
> Git commit message 建议：`feat(day23): fastapi chat api`

---

## 必做 A · API 验收（35 分）

1. 启动 `bash run.sh`，访问 `/docs` 截图。  
2. 用 curl 或 `/docs` 完成三类场景各一次：
   - 天气查询  
   - 订单 ST-10086  
   - 问候「你好」  
3. 保存为 `homework/day23/api_demo.png` 与 `homework/day23/curl_log.txt`。  
4. `python3 verify_day23.py` 全绿截图 → `verify_ok.png`。

---

## 必做 B · Pydantic 实验（25 分）

在 `homework/day23/pydantic_notes.md` 回答：

1. 向 `POST /api/chat` 发送 `"message": ""` 或 `"message": "   "`，记录 HTTP 状态码与 `detail` 结构。  
2. `ChatResponse` 中 `tools_used` 默认值是什么类型？若省略该字段，响应是否仍合法？  
3. 为何在 `ChatRequest` 里用 `@field_validator` 而不是只在路由里 `strip()`？

---

## 必做 C · 路由扩展（30 分）

在 `homework/day23/main_patch.py` 或 fork 的 `code/main.py` 中新增：

```python
@app.get("/api/tools", response_model=list[str])
def list_tools():
    """返回已注册工具名列表。"""
    ...
```

要求：

- 调用 `chat_service` 或 `ToolRegistry` 获取工具名  
- 在 `/docs` 可试调  
- 提交 diff 或完整文件  

---

## 选做 D · Day 22 真联调

1. 修改 `day22/static/app.js`：`USE_MOCK=false`，`API_BASE='http://127.0.0.1:8000'`。  
2. 双终端启动 preview + uvicorn。  
3. 截图 Network 面板中 `POST /api/chat` 请求与响应。  
4. 在 `homework/day23/integration_notes.md` 记录遇到的 CORS 问题（若有）。

---

## 选做 E · live 模式

配置 `.env`：

```env
SPARKTECH_MOCK=0
OPENAI_API_KEY=sk-...
```

对比 mock 与 live 回复差异，写入 `homework/day23/live_compare.md`（注意打码 Key）。

---

## 选做 F · OpenAPI 导出

```bash
curl -s http://127.0.0.1:8000/openapi.json | python3 -m json.tool > openapi.json
```

在 `homework/day23/openapi_notes.md` 说明：

- `paths` 里有哪些端点  
- `ChatRequest` 在 schema 中的 `required` 字段  

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `api_demo.png` | A |
| `curl_log.txt` | A |
| `verify_ok.png` | A |
| `pydantic_notes.md` | B |
| `main_patch.py` 或 `main.py` | C |

---

**截止时间**：Day 24 上午 09:00 前 push。
