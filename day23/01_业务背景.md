# Day 23 业务背景 · 后端交付 Chat API 给前端联调

## 1. 事件回顾

**事件单号**：API-CHAT-DELIVERY-2026-0723  
**发起人**：张工 + 产品小陈  
**关联**：Day 21 综合助手、Day 22 静态 Chat UI、Day 24 前后端联调  
**严重程度**：P0（阻塞 Day 24 fetch 联调排期）  
**截止日期**：今日 17:00 提交可运行的 `POST /api/chat` + Swagger 文档

### 1.1 现场情况

周二站会，设计部 Lisa 反馈：

| 现象 | 客户反应 | 技术根因 |
|------|----------|----------|
| 静态页能打字出字 | 「界面不错」 | Day 22 mock 成功 |
| 数据是写死的 | 「能接我们订单系统吗？」 | 缺真实 HTTP API |
| 演示录屏 | 产品要求明天联调 | Day 24 硬 deadline |

张工在工单评论：

> 「Day 22 前端已经把 **插头** 焊好了：`fetchChat` 调 `POST /api/chat`。  
> 今天后端组交付 **插座**——FastAPI 服务、Pydantic 校验、mock 工具文案与 Day 21 一致。  
> **先非流式**，流式 SSE Day 24 再上。」

### 1.2 为何今天系统学 FastAPI

```text
  Day 12  HTTP 概念（理论）
      │
  Day 20  FastAPI + SSE 预习（片段）
      │
  Day 21  CLI 综合助手（进程内）
      │
  Day 22  静态前端 + fetch 占位
      │
      ▼
  Day 23  ★ FastAPI 系统课 + Chat REST API
      │
      ▼
  Day 24  fetch 联调、CORS 实战
```

后端培训生此前主要写 **脚本和 REPL**，今日第一次把 LLM 能力 **暴露为 HTTP 服务**——这是企服项目从 Demo 到集成的关键一步。

## 2. 今日你在项目中的任务

```text
  上午  FastAPI 路由 / 路径参数 / 查询参数 / 自动文档
        │
        ▼
  上午  Pydantic：ChatRequest、ChatResponse、校验错误 422
        │
        ▼
  下午  chat_service.py：会话 + 工具 mock + 可选 live
        │
        ▼
  下午  main.py：POST /api/chat + CORSMiddleware
        │
        ▼
  傍晚  verify_day23.py + /docs 截图发群
        │
        ▼
  Day 24  前端 USE_MOCK=false 真联调
```

干系人：

1. **张工**：验收 API 契约与 Day 22 `app.js` 字段一致  
2. **Lisa / 前端同学**：需要 Swagger 文档与示例 JSON  
3. **QA 小陈**：无 Key 环境验收脚本全绿  
4. **客户成功**：需要 curl 命令证明「后端已就绪」  

## 3. 与大模型开发的隐性联系

| 今日知识点 | 日后场景 |
|------------|----------|
| REST API 封装 | 微服务、BFF、OpenAPI 契约 |
| Pydantic 校验 | 防 prompt 注入、字段长度限制 |
| `session_id` 会话 | 多用户隔离、Redis 会话存储 |
| `tools_used` 回传 | 可观测性、客服质检 |
| CORS | 浏览器联调必备 |
| mock 模式 | CI/CD、演示环境无 Key 部署 |

## 4. 今日学习目标（业务语言）

1. 能独立启动 FastAPI 并阅读 `/docs` 自动文档  
2. 能用 Pydantic 定义请求体与响应体，理解 422 校验错误  
3. 能把 Day 21 工具逻辑包进 `POST /api/chat`  
4. 能说明 CORS 为何需要、由谁配置  
5. 交付可供 Day 24 直接联调的 API 服务  

## 5. 风险与约束

| 风险 | 缓解 |
|------|------|
| 学员未写过 Web 框架 | 提供完整 `code/` 参考实现 |
| 字段与前端不一致 | `02_需求文档.md` 锁定契约 |
| 无 API Key | 默认 `SPARKTECH_MOCK=1` |
| 过早做流式 | `stream: true` 返回 501，Day 24 实现 |
| 端口冲突 | 默认 `8000`，文档写明 |

---

**一句话**：给 Day 22 的静态页一根 **能通电的线**，给 Day 24 留一个 **已经跑通的非流式接口**。
