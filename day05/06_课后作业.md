# Day 5 课后作业

## 必做 B1 · 字典 CRUD 练习

在 `homework/dict_hw.py` 中完成：

1. 创建空 dict `emp`，依次写入 Day 1 六个字段  
2. 用 `.get()` 读取不存在的 `email`，默认值 `"未填写"`  
3. 用 `.pop()` 删除 `need_dorm` 再重新写入  
4. 打印 `len(emp)` 与全部 `items()`

## 必做 B2 · 嵌套 JSON 手撕

不运行程序，阅读 `mock_api_response.json`，手写答案：

1. `data.pagination.total` 的值是多少？  
2. 第三条员工的 `need_dorm` 是什么类型？  
3. 画出从根到 `employees[2].phone` 的访问路径（Python 表达式）

## 必做 B3 · 扩展 parse_api

在课堂 `parse_api.py` 基础上增加函数 `get_pagination(root) -> dict`，安全返回分页信息；`main` 中打印 `page/total`。

## 必做 B4 · 导出增强

修改 `export_employees_json.py`（或复制为 `homework/export_hw.py`）：

1. 增加「清洗报告」：每条打印 `工号: 清洗前 phone -> 清洗后 phone`（仅 phone 有变化时）  
2. 在 JSON 顶层增加 `export_meta` 对象：`exported_at`（ISO 时间字符串）、`count`（条数）  
3. 通过 PRD AC-03 全部样例  

## 必做 B5 · JSON 字符串互转

`homework/json_roundtrip.py`：

```python
text = '{"name": "测试", "active": true, "score": null}'
```

1. `json.loads` 得到 dict，打印各值及 `type()`  
2. 修改 `score` 为 `100`，再 `dumps` 回字符串并打印  
3. 注释说明：为何不能把 `true` 直接写在 Python dict 字面量里

## 必做 B6 · 串联 Day 1 卡片字段

写 `homework/card_to_dict.py`：模拟 Day 1 六个变量，组装成一个 `employee` dict，再 `dumps` 为一行 JSON（`indent=None`），模拟 API 请求体单行格式。

## 选做 X1 · 过滤导出

仅导出 `need_dorm == true` 的员工到 `employees_dorm.json`。

## 选做 X2 · 统计部门

用 dict 统计各部门人数，打印 `{"大模型应用开发部": 3, ...}`。

## 挑战 C1 · 不用 json 模块

仅用 `ast.literal_eval` 能否解析本课 Mock 文件？试分析为何生产环境仍应用 `json`。（提示：布尔与 null）

## 挑战 C2 · 合并去重

若 Mock 中 `employee_id` 重复，保留后出现的记录并打印警告。在 `export` 中实现。

---

提交：`homework/day05/你的姓名/`，commit `homework(day05): 姓名`

验收：王工会用 `python3 -m json.tool output/employees_clean.json` 检查格式。
