# Day 48 流程图与示意图

## 1. Agent 协作总览

```mermaid
sequenceDiagram
    participant U as 用户
    participant P as Planner
    participant R as Researcher
    participant W as Writer
    participant T as Tools

    U->>P: 自然语言任务
    P->>P: 生成 plan[]
    P->>R: user_request + plan
    R->>T: document_rag_search
    R->>T: web_search
    T-->>R: hits + results
    R->>W: research_summary
    W->>T: email_draft
    T-->>W: draft_id
    W-->>U: 待审批预览 (Day49)
```

## 2. 工具调用分层

```
┌─────────────────────────────────────┐
│           Agent Layer               │
│  planner │ researcher │ writer       │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│           Tool Layer                │
│ calendar │ email │ search │ rag     │
│ task_list                           │
└─────────────────────────────────────┘
```

## 3. 敏感动作分类

| 类型 | 工具 | Day 48 | Day 49 |
|------|------|--------|--------|
| 只读 | list, rag, search | ✅ | ✅ |
| 草稿 | email_draft | ✅ | ✅ |
| 写/外发 | email_send, schedule | 实现 | + interrupt |

## 4. 数据流（RAG）

```mermaid
flowchart LR
    Docs[data/sample_docs] --> Index[build_rag_index]
    Query[用户问题] --> Search[document_rag_search]
    Index --> Search
    Search --> Hits[hits with score]
    Hits --> Researcher
```

## 5. Day 48 → Day 49 衔接

```mermaid
flowchart LR
    D48[工具+Agent节点] --> D49[office_graph.py]
    D49 --> API[FastAPI /api/tasks]
    D49 --> UI[审批前端]
```

---

*Day 48 流程图 · 可贴 PPT*
