# Day 48 课后作业

## 必做（B1–B3）

### B1 · 补充样本文档

在 `data/sample_docs/` 新增 `travel_policy.md`（≥200 字），内容包含「差旅审批」「机票超标」等关键词。  
验证：`document_rag_search("差旅 审批")` 能命中新文档。

### B2 · 扩展 calendar_list 过滤

为 `calendar_list` 增加可选参数 `keyword: str | None`，按 title 子串过滤事件。  
更新 `verify_project3.py` 或自写 `test_calendar_filter.py` 断言。

### B3 · 提交规范

```bash
git add day48/
git commit -m "day48: project3 tools and agents baseline"
```

## 选做（X1–X2）

### X1 · 第四 Agent「合规审查」

新增 `agents/compliance.py`：检查邮件正文是否含「机密」「薪资」等敏感词，命中则 `warnings[]`。  
不要求接入 graph，能单测即可。

### X2 · live web_search

接入 Serper 或 Tavily（需 API Key），`SPARKTECH_MOCK=0` 时走真实搜索。  
`.env.example` 补充 `SERPER_API_KEY`。

## 挑战（C1）

将 `mock_plan` 改为调用 OpenAI 兼容 API 输出 JSON `{"steps": [...]}`，失败时 fallback 到规则 mock。

---

**提交**：作业说明 + 关键截图 + verify 输出  
**截止**：Day 49 课前
