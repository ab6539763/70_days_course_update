# Day 14 课后作业 · Code Review 清单与扩展

> 今日主课已是全天项目；作业侧重 **互评 + 自检**。答辩前请逐项勾选。

---

## Part A · 答辩前 Code Review 清单（必做）

复制到你的 `homework/day14/REVIEW.md`，逐项打勾。

### A1. 结构与模块

- [ ] `project1/` 含 6 个模块文件 + `__init__.py`
- [ ] 各模块职责符合 [03_架构与设计.md](./03_架构与设计.md) 职责表
- [ ] 无「上帝文件」：`main.py` < 120 行（参考线）
- [ ] `sparktech/` 含 `exceptions.py` 与 `utils/`

### A2. ChatMessage & Session

- [ ] `role` 仅允许 system/user/assistant
- [ ] 空 `content` 抛 `ValueError`
- [ ] `ConversationSession` 初始化含默认 system
- [ ] `to_api_messages()` 不含 `created_at`
- [ ] `clear(keep_system=True)` 行为正确

### A3. LLM Client

- [ ] 无 Key 时 `is_mock_mode == True`
- [ ] `chat()` 接收 `list[dict]` 而非单字符串
- [ ] mock 回复能体现 **消息条数** 或上下文
- [ ] live 模式捕获 API 异常为 `LLMClientError`
- [ ] LLM 失败时 **不** 向 session 追加 assistant 消息

### A4. 命令与 REPL

- [ ] `/help` `/clear` `/save` `/exit` 全部可用
- [ ] 未知命令有提示且不退出
- [ ] `/save` 可指定文件名
- [ ] Ctrl+C 不直接崩溃
- [ ] 空行被忽略

### A5. 持久化

- [ ] `data/sessions/` 自动创建
- [ ] JSON 含 `session_id`, `messages`, 时间戳
- [ ] `load_session` 可还原会话（自测或选做）
- [ ] 损坏 JSON 抛 `DataLoadError` 或 `SessionStorageError`

### A6. 工程规范

- [ ] `requirements.txt` 可安装
- [ ] `.env.example` 无真实 Key
- [ ] `bash run.sh` 或等效命令可启动
- [ ] 类型注解覆盖公共方法
- [ ] Git commit message 含 `day14`

### A7. 互评（两人一组）

| 评项 | 评分 1-5 | 备注 |
|------|----------|------|
| 可读性 | | |
| 异常处理 | | |
| PRD 符合度 | | |
| 演示流畅度 | | |

---

## Part B · 必做扩展（B1）

实现 **`/history`** 命令：打印当前会话所有消息的 `__str__`，不调用 LLM。

验收：

```text
你> /history
[system] 你是星火智服...
[user] 你好
[assistant] [mock] ...
```

提示：在 `commands.py` 增加 `_cmd_history`。

---

## Part C · 选做

### C1. `/load <file>`

从 `data/sessions/` 加载 JSON 替换当前 session。注意处理文件不存在。

### C2. 会话标题自动更新

首轮 user 消息前 20 字设为 `session.title`。

### C3. 真实 API 对比截图

同一句 prompt 分别 mock / live，写 200 字对比到 `homework/day14/mock_vs_live.md`。

### C4. 最小 pytest

```bash
pytest homework/day14/test_models.py -q
```

覆盖：`ChatMessage` 非法 role、`save_session` 往返。

---

## Part D · 挑战

### D1. 命令行参数

```bash
python -m project1.main --session demo.json   # 启动时加载
python -m project1.main --no-mock             # 强制 live
```

使用 `argparse`，不破坏默认 REPL 行为。

### D2. 多会话管理

维护 `sessions: dict[str, ConversationSession]`，新增 `/switch <id>`。

---

## 提交要求

```text
homework/day14/
├── REVIEW.md           # Part A 勾选结果
├── B1_history/         # 或直接在 project1 分支
│   └── commands.py     # 含 /history
└── README.md           # 如何运行你的扩展
```

邮件/群提交：**仓库链接 + 1 个 session JSON + 互评表截图**。

---

**截止**：答辩前一日 22:00
