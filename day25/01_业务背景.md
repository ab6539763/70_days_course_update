# Day 25 业务背景 · 星火智服 Phase2 迁移 LangChain 栈

## 1. 事件回顾

**事件单号**：PHASE2-LC-MIGRATE-2026-0725  
**发起人**：张工 + 架构组  
**关联**：[Day 14 CLI](../day14/)、[Day 24 Web Chat](../day24/)  
**严重程度**：P0（Phase2 技术栈统一里程碑）  
**截止日期**：今日 17:00 提交 LangChain 版 CLI + `verify_day25.py` 全绿

### 1.1 现场情况

Week 5 启动会，客户成功反馈：

| 现象 | 业务影响 | 技术根因 |
|------|----------|----------|
| CLI 与 Web 两套 LLM 封装 | 维护成本高 | Day 14 手写 vs Day 24 另一套 client |
| 新人上手慢 | 培训周期拉长 | 缺少统一框架抽象 |
| 后续要上 RAG/Agent | PoC 难扩展 | 需要 LangChain 生态 |

张工在工单评论：

> 「Phase2 第一步：**把 Day 14 `project1` 迁到 LangChain**。  
> 保持 `/help` `/clear` `/save` `/exit` 不变；mock 文案与 Day 14 对齐。  
> Day 24 Web 本周不动，但学员要理解 **明天 LCEL、后天 Memory** 如何接 Web。」

### 1.2 为何今天学 LangChain 入门

```text
  Day 14  手写 LLMClient + messages dict
      │
  Day 24  Web SSE + SQLite（仍手写 client）
      │
      ▼
  Day 25  ★ LangChain ChatModel + PromptTemplate
      │
      ▼
  Day 26  LCEL 管道 | OutputParser
      │
      ▼
  Day 27  Memory 多轮持久化
      │
      ▼
  Week 6  Web 后端统一迁入 LangChain
```

此前课程（Day 19 PRD）刻意 **禁止 import langchain**，是为了先理解底层 HTTP 与消息结构。Phase2 起 **允许并鼓励** 使用 LangChain 作为标准栈。

## 2. 今日你在项目中的任务

```text
  上午  LangChain 架构：core / community / openai 分包
        │
        ▼
  上午  PromptTemplate vs ChatPromptTemplate
        │
        ▼
  下午  langchain_chat.py：Day 14 REPL 功能对齐
        │
        ▼
  下午  FakeListChatModel mock 模式验收
        │
        ▼
  傍晚  verify_day25.py + 与 Day 14 对照表截图
```

干系人：

1. **张工**：验收 CLI 命令与 Day 14 一致  
2. **QA 小陈**：无 Key 环境脚本全绿  
3. **培训生**：理解 ChatModel 抽象，为 LCEL 铺路  
4. **前端 Lisa**：了解 Phase2 不影响 Day 24 Web 演示  

## 3. 与大模型开发的联系

| 今日知识点 | 日后场景 |
|------------|----------|
| `ChatModel` 统一接口 | 切换 OpenAI / 国产模型 / 本地模型 |
| `ChatPromptTemplate` | 多轮对话、RAG 上下文注入 |
| `FakeListChatModel` | CI 无 Key 集成测试 |
| 消息类型 System/Human/AI | 与 OpenAI API messages 一一对应 |

## 4. 成功标准（业务视角）

- 培训生能在白板画出：**用户输入 → PromptTemplate → ChatModel → 回复**  
- `langchain_chat.py` 可替代 Day 14 `bash run.sh` 做课堂演示  
- 产品确认：斜杠命令与 JSON 会话格式 **向后兼容** Day 14  

---

**下一课预告**：Day 26 用 `prompt | llm | parser` 重构翻译与结构化输出链。
