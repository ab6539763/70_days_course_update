# Day 25 课后作业

> 提交目录：`homework/day25/`  
> Git commit message 建议：`feat(day25): langchain cli migration`

---

## 必做 A · 验收与对照（30 分）

1. 运行 `python3 code/verify_day25.py`，截图保存为 `homework/day25/verify_ok.png`。  
2. 分别启动 [Day 14](../day14/) 与 Day 25 REPL，各完成 2 轮对话 + `/save`，截图对比。  
3. 填写 `homework/day25/compare_day14_day25.md`（模板见下）。

**compare 模板**：

```markdown
| 功能 | Day 14 实现位置 | Day 25 实现位置 | 是否一致 |
|------|-----------------|-----------------|----------|
| mock 模式 | | | |
| /clear | | | |
| JSON 格式 | | | |
```

---

## 必做 B · PromptTemplate 实验（35 分）

在 `homework/day25/prompt_experiment.py` 中：

1. 新增一个 `PromptTemplate`，输入变量为 `ticket_id` 和 `tone`，模板要求「用 {tone} 语气总结工单 {ticket_id}」。  
2. 新增一个 `ChatPromptTemplate`，在 system 中注入公司名称「星火智服」。  
3. 打印 `format_messages` 结果到 `homework/day25/prompt_output.txt`。

---

## 必做 C · system 配置外置（35 分）

1. 在 `homework/day25/system_prompts.json` 定义至少 2 套 system（如 `default`、`training`）。  
2. 修改或扩展 `langchain_chat.py`，支持环境变量 `SPARKTECH_SYSTEM_PROFILE=training` 切换。  
3. README 说明切换方式。

---

## 选做 D · 阅读 Day 24（10 分加分）

阅读 [Day 24 backend/llm_client.py](../day24/backend/llm_client.py)，在 `homework/day25/web_migration_notes.md` 写：

- 哪些函数可用 LangChain `ChatModel` 替换？  
- SSE 流式在 LangChain 中对应什么方法？（提示：`stream`）

---

## 提交清单

- [ ] `verify_ok.png`  
- [ ] `compare_day14_day25.md`  
- [ ] `prompt_experiment.py` + `prompt_output.txt`  
- [ ] `system_prompts.json` + 切换说明  
- [ ] （选做）`web_migration_notes.md`
