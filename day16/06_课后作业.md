# Day 16 课后作业

> 提交目录：`homework/day16/`  
> Git commit message：`feat(day16): 采样参数与流式演示作业`

---

## 必做 A · 角色消息（25 分）

在 `homework/day16/my_system_prompt.py` 编写：

1. 专为「星火智服退款咨询」设计的 system 提示（≥ 80 字）  
2. 含 2 轮历史 + 最新 user 问题  
3. 调用 `llm_compat.get_llm_client().chat(messages)` 打印回复  

`notes.md` 说明：若去掉 system，mock 回复有何不同（描述即可）。

---

## 必做 B · 流式体验（25 分）

在 `homework/day16/my_stream.py`：

```python
for chunk in client.chat_stream(messages, temperature=0.3):
    ...
```

要求：实现 `typewriter` 效果，并统计 **首 chunk 到首字符打印** 的耗时（mock 也算）。

---

## 必做 C · 参数对比（30 分）

1. 在 `demo_prompts.json` 新增自定义 `prompt_id`  
2. 运行 `compare_params.py` 生成报告  
3. 在 `homework/day16/param_analysis.md` 写 200 字结论：推荐哪组 preset，为什么  

---

## 必做 D · 默认配置（20 分）

将最终推荐参数写入 `homework/day16/demo_defaults.json`，字段：

- `temperature`, `top_p`, `max_tokens`, `frequency_penalty`, `stream`  
- `rationale`（中文一句）  
- `linked_client`（day12/13/14 路径说明）

---

## 选做 E · 接入 Day 14 project1

修改 `day14/project1/llm_client.py` 或配置，读取 `demo_defaults.json` 作为默认 temperature（提交 patch 说明）。

---

## 选做 F · max_tokens 截断实验

`param_experiment.py --param max_tokens` 截图：max_tokens=20 时回复是否被截断。

---

## 挑战 G · 双参数热力图（文字版）

对 temperature ∈ {0,0.5,1} 与 frequency_penalty ∈ {0,0.5,1} 共 9 组跑同一 prompt，用 Markdown 表格评价「稳定性/多样性/长度」三星。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `my_system_prompt.py` + `notes.md` | A |
| `my_stream.py` | B |
| `param_analysis.md` | C |
| `demo_defaults.json` | D |
| compare 报告 JSON 副本 | C |

---

**截止**：Day 17 上午 09:00
