# Day 34 课后作业

## 作业说明

- 完成后提交至 `homework/day34/`  
- Git commit message 含 `day34-homework`  
- 必做预计 2h，选做 1h，挑战 2h+

---

## 必做（100 分）

### B1 · 跑通验收（20 分）

```bash
cd day34/code && python3 verify_day34.py
```

截图「8/8 通过」。

### B2 · 扩展测试集（30 分）

1. 在 `code/data/` 新增 `kb_shipping.md`（物流 FAQ，至少 2 个 `##` 节）  
2. 运行 `build_test_set.py`  
3. 确认 `sample_qa_pairs.json` 新增条目且 `id` 不重复  

### B3 · 评估报告（30 分）

运行：

```bash
python3 ragas_eval.py --force-mock --limit 8 --json > report.json
```

写 200 字小结：哪项指标最低？可能原因？Day 36 如何改进？

### B4 · 概念题（20 分）

用自己的话解释（各 50 字内）：

1. faithfulness 与 answer relevance 区别  
2. context precision 低说明什么问题  

---

## 选做（+20 分）

### X1 · 对接 Day 33 真实 RAG

若你已完成 Day 33，导出 3 条真实 `(question, answer, contexts)`，调用 `evaluate_rag_sample` 并对比 mock 流水线分数。

### X2 · 安装 Ragas live

```bash
pip install ragas datasets
```

配置 Key 后跑 `python3 ragas_eval.py --limit 3`，对比 mock 与 Ragas 分数差异。

---

## 挑战（+30 分）

### C1 · 幻觉检测样例

手工构造一条 **faithfulness 明显偏低** 的样本（答案与 context 矛盾），写入 `homework/day34/hallucination_case.json` 并跑分截图。

### C2 · CI 脚本

写 `homework/day34/ci_eval.sh`：无网络时 `--force-mock`，分数低于阈值（自定）时 exit 1。

---

**提交清单**：截图 + `report.json` + 小结 Markdown +（选做/挑战附加文件）
