# Day 34 · RAG 评估（mock 模式无需 Key）
# 若使用 Ragas live 评估，配置 OpenAI 兼容 API
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# 强制启发式评估（不调用 Ragas / LLM）
RAG_EVAL_FORCE_MOCK=1
