# Day 31 流程图与示意图

## 1. 调参实验总流程

```mermaid
flowchart TD
    A[加载 sparktech_kb.txt] --> B[网格: chunk × top_k × model]
    B --> C[TextSplitter 切块]
    C --> D[Embedding 向量化]
    D --> E[对 EVAL_QUERIES 检索]
    E --> F[计算 hit_rate / score]
    F --> G{还有组合?}
    G -->|是| B
    G -->|否| H[输出报告 + JSON]
```

## 2. 检索单链路

```ascii
  User Query: "怎么申请 API Key？"
        │
        ▼
  ┌─────────────┐
  │ embed_query │
  └──────┬──────┘
         │
         ▼
  ┌──────────────────────────────┐
  │  For each chunk: cosine sim  │
  └──────────────┬───────────────┘
                 │
                 ▼
  ┌──────────────────────────────┐
  │  Sort desc → take top_k      │
  └──────────────┬───────────────┘
                 │
                 ▼
  hit_at_k?  keywords in top_k chunks
```

## 3. 参数影响示意

```mermaid
graph LR
    subgraph chunk_size
        S[256 细粒度]
        M[512 平衡]
        L[1024 粗粒度]
    end
    subgraph top_k
        K3[k=3 少噪声]
        K5[k=5 默认]
        K10[k=10 高召回]
    end
    S --> K5
    M --> K5
    L --> K3
```

## 4. Week 4 → Day 31 位置

```text
  Day 25 ── Day 26 ── Day 27 ── Day 28 ── Day 29 ── Day 30
                                                      │
                                                      ▼
                                              Day 31 测验+调参
                                                      │
                                    ┌─────────────────┴─────────────────┐
                                    ▼                                   ▼
                              Day 32 高级 RAG                    Day 33 Hybrid
```
