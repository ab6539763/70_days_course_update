# Day 31 业务背景 · Week 4 结业考与 RAG 调参危机

## 1. 事件回顾

**事件单号**：MIL-W4-RAG-TUNE-2026-0731  
**发起人**：李姐（算法）+ QA 小陈  
**关联**：Day 25–30 RAG 专题、Day 20 Embedding  
**严重程度**：P0（阻塞高级 RAG 专题排期）  
**截止日期**：今日 17:00 提交测验答卷 + 调参实验报告

### 1.1 现场情况

周四预发环境 RAG 问答抽检，客服知识库上线后召回率不稳定：

| 现象 | 用户问句 | 检索结果 | 根因初判 |
|------|----------|----------|----------|
| 答非所问 | 「API Key 怎么申请」 | 返回企业定价段落 | chunk 过大，语义稀释 |
| 漏召回 | 「能退钱吗」 | 无退款相关块 | chunk 过小 + top_k 太低 |
| 模型差异 | 同一问句换 embedding | 排序完全不同 | 未做 embedding 对比实验 |

李姐在工单评论：

> 「Week 4 不是再加一个 RAG demo，是 **结业考 + 调参实验**：  
> 上午 `week4_review_quiz` 检验概念；下午 `rag_tuning_lab` 网格搜索 chunk / top_k / embedding。  
> 别凭感觉改参数——用 hit_rate 说话。」

### 1.2 Week 4 知识地图（业务视角）

```text
  Day 25  向量数据库入门
      │
  Day 26  文档加载与清洗
      │
  Day 27  基础 RAG 检索链
      │
  Day 28  LangChain 组件拼装
      │
  Day 29  Chroma 持久化索引
      │
  Day 30  完整 RAG 问答链路
      │
      ▼
  Day 31  ★ 测验 + rag_tuning_lab 调参实验
```

## 2. 今日你在项目中的任务

```text
  上午  week4_review_quiz 闭卷/开卷测验
        │
        ▼
  上午  讲评：chunk / top_k / embedding 原理
        │
        ▼
  下午  rag_tuning_lab 网格搜索实验
        │
        ▼
  下午  输出推荐配置 + verify_day31 全绿
        │
        ▼
  Day 32  查询改写 / Multi-Query / HyDE
```

## 3. 业务验收口径

| 角色 | 关注点 |
|------|--------|
| 李姐 | 实验可复现、指标可解释 |
| 小陈 | 测验 ≥80%，verify 全绿 |
| 张工 | mock 优先，结果 JSON 可归档 |

## 4. 风险与约束

- 无 API Key：必须 mock 模式跑通全流程  
- 语料固定为 `sparktech_kb.txt`，便于横向对比  
- 实验结果写入 `code/output/`，便于 Git 忽略大文件

---

**下一课预告**：Day 32 解决「用户问法与文档措辞不一致」——查询改写、多查询、HyDE。
