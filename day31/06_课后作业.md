# Day 31 课后作业

## 必做（截止：次日上午 10:00）

1. **测验订正**：`week4_review_quiz` 错题 ≥3 条，写入笔记或 `homework/day31/quiz_notes.md`。  
2. **实验复现**：在本地跑通 `rag_tuning_lab.py`，记录推荐配置三项参数。  
3. **验收截图**：`verify_day31.py` 全绿截图提交到学习群。

## 选做

4. 增加第四维参数 `chunk_overlap`（128 vs 64），观察 hit_rate 变化。  
5. 用 matplotlib 绘制 chunk_size × top_k 的 hit_rate 热力图（固定一个 embedding）。  
6. 阅读 LangChain 文档中 `CharacterTextSplitter` 与 `RecursiveCharacterTextSplitter` 差异，写 200 字对比。

## 挑战

7. 将实验台改为读取 Chroma 持久化索引（Day 29 作业），对比内存检索与向量库检索 latency。

## 提交规范

- Git commit message 含 `day31`  
- 可选：`homework/day31/report.md` 含配置表与结论
