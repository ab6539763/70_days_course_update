# Day 22 课后作业

> 提交目录：`homework/day22/`  
> Git commit message 建议：`feat(day22): static chat prototype`

---

## 必做 A · 静态原型验收（40 分）

1. 运行 `bash preview.sh`，完成 3 轮对话截图。  
2. 保存为 `homework/day22/demo_screenshot.png`。  
3. 在 `homework/day22/checklist.md` 勾选 PRD 全部 P0 验收项。

---

## 必做 B · 样式调整（30 分）

在 `homework/day22/notes.md` 记录你做的 **2 处** 样式改动，例如：

- 修改 `--primary` 色值  
- 调整气泡 `border-radius`  
- 增加 header 阴影  

并附改动前后对比截图（可小图）。

---

## 必做 C · mock 扩展（30 分）

在 `homework/day22/app_patch.js` 或直接在 fork 的 `static/app.js` 中增加第 4 条 mock：

- 关键词：`帮助` / `help`  
- 回复：列出本系统支持的查询类型（天气、订单、问候）

提交 diff 或完整 `app.js` 到作业目录。

---

## 选做 D · localStorage 会话

刷新页面后保留历史消息：

```javascript
const STORAGE_KEY = 'sparktech_chat_v1';
// saveMessages() / loadMessages()
```

---

## 选做 E · 无障碍

1. 为发送按钮加 `aria-label="发送消息"`  
2. 清空按钮加确认 `confirm()`  
3. 在 `notes.md` 说明键盘 Tab 顺序  

---

## 选做 F · fetch 干跑

不启后端，在 `notes.md` 写：

- `fetchChat` 各参数含义  
- 预期 Day 24 FastAPI 返回 JSON 样例  
- CORS 是什么、谁配置  

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `demo_screenshot.png` | A |
| `checklist.md` | A |
| `notes.md` | B |
| `app_patch.js` 或 `app.js` | C |

---

**截止时间**：Day 23 上午 09:00 前 push。
