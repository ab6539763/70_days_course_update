# -*- coding: utf-8 -*-
"""
星火科技 · 培训 Day 1
模块：constants.py
说明：存放入职信息卡片相关的业务常量与枚举值。

设计意图（张工评审）：
    - 把「会变的配置」从主逻辑里拆出来，方便 Day 10 拆包时直接 import
    - 部门列表与 PRD 02_需求文档.md 第 3.1 节保持一致

作者：培训生（请改成你的姓名）
日期：2026-07-05
"""

# ---------------------------------------------------------------------------
# 公司与展示相关常量
# ---------------------------------------------------------------------------

# 公司名称：出现在卡片标题与页脚，修改此处即可全局生效
COMPANY_NAME: str = "星火科技"

# 卡片横幅副标题
CARD_SUBTITLE: str = "入职信息卡片"

# 分隔线宽度（字符数）。PRD 原型约 50 列，终端过窄时可改为 40
CARD_WIDTH: int = 50

# 单条分隔线重复使用的字符
LINE_CHAR_MAIN: str = "="
LINE_CHAR_SUB: str = "-"


# ---------------------------------------------------------------------------
# 业务枚举：允许选择的部门（v1.0 用列表展示，Day 4 可改为 input 校验）
# ---------------------------------------------------------------------------

DEPARTMENTS: tuple[str, ...] = (
    "大模型应用开发部",
    "智能应用事业部-交付组",
    "智能应用事业部-产品组",
    "人力资源部",
    "信息技术部",
)

# 默认部门：培训生绝大多数归属此部门
DEFAULT_DEPARTMENT: str = "大模型应用开发部"


# ---------------------------------------------------------------------------
# 工号相关（v2 可对接 OA 自动生成，Day 1 允许手动输入或默认值）
# ---------------------------------------------------------------------------

# 工号前缀，符合星火科技编码规范 ST = SparkTech
EMPLOYEE_ID_PREFIX: str = "ST"

# 示例工号，US-04 工号只读默认值场景可在主程序中引用
SAMPLE_EMPLOYEE_ID: str = "ST-2026-001"


# ---------------------------------------------------------------------------
# 用户输入提示文案（集中管理，便于 Day 2 做多语言或文案 A/B）
# ---------------------------------------------------------------------------

PROMPT_NAME: str = "请输入姓名（2-20字）："
PROMPT_EMPLOYEE_ID: str = "请输入工号（如 ST-2026-001，直接回车使用示例工号）："
PROMPT_DEPARTMENT: str = "请输入部门（直接回车使用默认部门）："
PROMPT_ONBOARD_DATE: str = "请输入入职日期（YYYY-MM-DD）："
PROMPT_PHONE: str = "请输入 11 位手机号："
PROMPT_NEED_DORM: str = "是否住宿？(y/n)："


# ---------------------------------------------------------------------------
# 布尔展示文案（PRD AC-04：界面显示「是/否」而非 True/False）
# ---------------------------------------------------------------------------

BOOL_DISPLAY_TRUE: str = "是"
BOOL_DISPLAY_FALSE: str = "否"
