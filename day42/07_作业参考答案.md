# Day 42 作业参考答案

## Checkpointer

- 同一 `thread_id`：连续 `invoke({})` 使 count 递增  
- 不同 `thread_id`：状态隔离，互不影响  
- 续跑勿传会覆盖 checkpoint 的字段（如 `count: 0`）

## HITL

```python
app.invoke(Command(resume="这是人工修改后的全文"), config)
# approved=False, revision_count=1, draft 为传入文本
```

## 并行 + 重试

- `research_notes` 长度 ≥ 2（searcher + analyst）  
- mock 下前两次 `quality_score=0.4`，第三次 `0.85`，`retry_count=2`  
- `status` 为 `已发布`

## SqliteSaver（选做）

```python
from langgraph.checkpoint.sqlite import SqliteSaver
with SqliteSaver.from_conn_string("checkpoints.db") as cp:
    app = graph.compile(checkpointer=cp)
```
