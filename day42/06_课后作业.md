# Day 42 课后作业

## 必做
1. 跑通三个脚本 + `verify_day42.py` 全绿  
2. 修改 `thread_id`，观察 `checkpoint_threads.json` 差异  
3. 在 `human_in_loop.py` 中 resume 传入修改后的 draft 文本，记录 `revision_count` 变化  

## 选做
4. 将 `MemorySaver` 换为 `SqliteSaver`（`langgraph-checkpoint-sqlite`）  
5. 为 `writing_agent_graph` 增加第四个并行节点 `fact_check`  

## 挑战
6. 用 FastAPI 暴露 `/graph/resume` 接口：GET 状态 + POST `Command(resume=...)`（mock 即可）
