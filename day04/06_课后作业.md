# Day 4 课后作业

## 必做 B1 · 列表基础

完成 `homework/list_hw.py`：

1. 创建列表 `sprint = ["接口文档", "单测", "验收"]`  
2. `append` 添加 `"部署脚本"`  
3. 用切片取出前两项  
4. 用 `pop` 删除最后一项并打印被删元素  

## 必做 B2 · 排序与推导式

`homework/comprehension_hw.py`：

给定：

```python
todos = [
    {"id": 1, "title": "A", "done": False, "priority": "P2"},
    {"id": 2, "title": "B", "done": True, "priority": "P0"},
    {"id": 3, "title": "C", "done": False, "priority": "P1"},
]
```

1. 推导式得到所有未完成标题列表  
2. `sorted` 按 priority（P0 < P1 < P2）输出 title  
3. 统计已完成数量（提示：`sum(1 for t in todos if t["done"])`）  

## 必做 B3 · 元组与集合

`homework/tuple_set_hw.py`：

1. 用 tuple 保存 `VERSION = (1, 0, 0)`，打印 `v1.0.0` 格式  
2. 输入标签字符串 `"api,doc,api,qa"`，用 set 去重后排序输出  
3. 两个标签集合求交集，模拟「同时含 api 与 qa 的任务过滤」  

## 必做 B4 · 待办管理器增强

在课堂 `todo_manager.py` 基础上增加：

- 菜单项 `5.stats`：打印总数、完成数、完成率（保留一位小数）  
- list 子选项：1 全部 / 2 未完成 / 3 已完成  
- complete 时若已完成则提示，不重复操作  

## 必做 B5 · 业务文档

写 `homework/sprint_notes.md`（不少于 200 字）：

- 描述你如何用今日工具准备一场虚构 Sprint 规划会  
- 说明 `complete` 与 `delete` 的业务区别  
- 预告 Day 5 将如何持久化  

## 选做 X1

实现 `undo`：用第二个 list 存被 delete 的项，支持恢复最近一条。

## 选做 X2

list 按标题关键词搜索（大小写不敏感），打印匹配项。

## 挑战 C1

不用 `sorted`，手写冒泡排序对 `["P2","P0","P1"]` 按 `PRIORITY_RANK` 排序（复习 Day 3 循环）。

## 挑战 C2

阅读 `03_架构与设计.md` Day 48 预留接口，写 `homework/agent_task_preview.py`：

- 函数 `tool_list_pending(todos) -> list[dict]`  
- 仅返回 `done is False` 的项，字段与 Day 4 一致  
- docstring 说明未来如何被 Agent 调用  

---

提交：`homework/day04/你的姓名/`，commit `homework(day04): 姓名`
