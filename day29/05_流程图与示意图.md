# Day 29 流程图与示意图

```mermaid
flowchart TB
    A[sample_docs] --> B[Loader]
    B --> C[Splitter]
    C --> D[Embeddings]
    D --> E[Chroma]
    E --> F[similarity_search]
    F --> G[Retriever]
    G --> H[Day30 LLM]
```

## 建库时序

```mermaid
sequenceDiagram
    participant B as build_vectorstore
    participant E as Embeddings
    participant C as Chroma

    B->>B: load + split
    B->>E: embed_documents(chunks)
    E-->>B: vectors
    B->>C: add_documents
    C-->>B: ids
```

## 检索时序

```text
query ──embed_query──▶ q_vec
                          │
chroma collection ◀───────┘
                          │
                    top-k by distance
                          │
                          ▼
                    list[Document]
```

---

*Day 29 图示 v1.0*
