# Day 29 业务背景 · 向量库入库与相似检索

## 1. 事件单

**事件单号**：KB-VECTOR-CHROMA-2026-0729  
**发起人**：刘姐 · 知识库运营  
**关联**：Day 28 切块 JSON、Day 30 RAG CLI  
**严重程度**：P0  

### 1.1 需求陈述

刘姐：

> 「昨天切块报告我看了，chunk_size=400 可以。今天把 **全部 sample_docs embed 进 Chroma**，  
> 我要在终端输入『怎么退款』，**第一条就是退款 FAQ**——不是关键词匹配，是 **语义检索**。」

### 1.2 今日目标

```text
  Day 28 切块
       │
       ▼
  Embedding（mock 或 live）
       │
       ▼
  Chroma 持久化
       │
       ▼
  similarity_search + Retriever
```

## 2. 干系人

| 角色 | 期望 |
|------|------|
| 刘姐 | 建库一键、检索可演示 |
| 张工 | mock/live 可切换 |
| 小陈 | 为 Day 30 生成阶段铺路 |

## 3. 学习目标

1. Chroma `persist_directory` 落盘位置  
2. LangChain `Embeddings` 接口  
3. `similarity_search_with_score` 解读分数  
4. `as_retriever()` 供 Day 30 链接 LLM  

---

*下一日：Day 30 KEY DAY 完整 RAG CLI。*
