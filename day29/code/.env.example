# Day 29 · Chroma 向量库（可选）
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# 无 Key 时自动 mock（CI 推荐）
SPARKTECH_MOCK=1

# Chroma 持久化目录（相对 code/）
CHROMA_PERSIST_DIR=./chroma_db
