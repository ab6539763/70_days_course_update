# Day 29 需求文档 · Chroma 向量库

| 属性 | 内容 |
|------|------|
| 编号 | PRD-SPARK-RAG-CHROMA-029 |
| 上游 | Day 28 sample_docs |
| 下游 | Day 30 `rag_pipeline.py` |

## 1. 目标

1. `mock_embeddings.py` 实现 LangChain Embeddings  
2. `chroma_kb.py` 封装增删查与 Retriever  
3. `build_vectorstore.py` 从 Day 28 目录建库  
4. `similarity_search_demo.py` ≥6 条 demo query  
5. `verify_day29.py` 无 Key 全绿  

## 2. 功能需求

### build_vectorstore.py

- 读取 `day28/code/data/sample_docs`  
- RecursiveCharacterTextSplitter: size=400, overlap=60  
- 输出 `output/build_report_*.json`  

### chroma_kb.py

- `add_documents` / `similarity_search` / `similarity_search_with_score`  
- `as_retriever(search_kwargs={"k": 4})`  
- `reset()` 教学重置  

### 验收

- chunks ≥ 5  
- 退款 query Top 命中含「退款」或「工单」  
- mock 向量：相近问句 dot product 更高  

---

*评审：张工 · 刘姐*
