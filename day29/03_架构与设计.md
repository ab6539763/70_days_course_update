# Day 29 架构与设计 · Chroma + Embedding + Retriever

## 1. 架构图

```text
sample_docs/
      │
      ▼
load_all_documents()  ──▶  raw Documents
      │
      ▼
RecursiveCharacterTextSplitter
      │
      ▼
get_embeddings()  ── mock: SparkMockEmbeddings
                └── live: OpenAI /embeddings
      │
      ▼
Chroma.from_documents / add_documents
      │
      ▼
persist_directory/chroma_db/
      │
      ▼
similarity_search_with_score(query)
      │
      ▼
as_retriever()  ──▶ Day 30 LLM
```

## 2. 分数换算

Chroma 返回 **距离** distance（越小越近）：

```python
similarity = 1.0 / (1.0 + distance)
```

Day 30 用 `RAG_SCORE_THRESHOLD` 过滤低分块。

## 3. 模块职责

| 模块 | 职责 |
|------|------|
| `mock_embeddings.py` | Embeddings 工厂 |
| `chroma_kb.py` | 向量库 CRUD + Retriever |
| `build_vectorstore.py` | 离线建库 |
| `similarity_search_demo.py` | 在线检索演示 |

---

*设计：张工*
