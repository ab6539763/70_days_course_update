# Day 29 课后作业

> 提交：`homework/day29/` · commit：`feat(day29): ...`

## 必做 A · 扩充建库（30 分）

将自有 5 篇文档加入 `homework/day29/docs/`，修改 `my_build.py` 建库，输出 `build_report.json`。

## 必做 B · 检索评测（40 分）

编写 `eval_retrieval.py`：

- 10 条 query + 期望来源文件  
- 统计 Recall@3  
- mock 模式 Recall@3 ≥ 50%  

## 必做 C · metadata 过滤（30 分）

Chroma 检索时只搜 `format=txt` 的块（`where` 过滤），写 `filtered_search_demo.py`。

## 选做 D

有 Key 时对比 mock vs `text-embedding-3-small` 的 Recall@3。

---

**截止**：Day 30 上午 09:00
