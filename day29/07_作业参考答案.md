# Day 29 作业参考答案

## A · my_build.py

复用 `build_vectorstore.load_all_documents` + `ChromaKnowledgeBase`，改 `docs_dir` 即可。

## B · eval_retrieval.py

```python
EXPECTED = [
    ("如何退款", "faq_refund.txt"),
    ("工单回复时效", "service_policy.txt"),
]
# Recall@3 = 命中数 / len(EXPECTED)
```

讲评：mock 向量对同义词敏感有限，可适当降低 threshold 或扩充主题词表。

## C · 过滤检索

```python
store.similarity_search(query, k=4, filter={"format": "txt"})
```

LangChain-Chroma 版本差异可用 `where` 文档查阅；教学版可在 `metadata` 手动过滤。

---

**结语**：检索质量决定 RAG 上限——Day 30 先保证「搜得到」。
