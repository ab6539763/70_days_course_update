# Day 33 课后作业

## 必做
1. 跑通三个脚本 + verify 全绿  
2. 对比纯向量 vs Hybrid 在同一查询上的 top-3 差异（写 3 条结论）  
3. 画图说明 Parent Document 如何解决「检索碎片」问题  

## 选做
4. 调整 `rrf_k`（30 vs 60 vs 120）观察排序变化  
5. 接入真实 `BAAI/bge-reranker-base`（需 HF 环境）  

## 挑战
6. 将 `enhanced_rag_pipeline` 封装为 FastAPI `/rag` 端点（mock 即可）
