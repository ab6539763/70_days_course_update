# Day 2 课后作业

## 必做 B1 · 运算符练习

完成 `homework/operators_hw.py`：实现「API 成本计算器」

- 输入：总 token 数（int）、每 1K token 价格（float）  
- 输出：`本次调用预估费用：X.XX 元`（f-string 保留两位小数）

## 必做 B2 · 字符串练习

`homework/string_hw.py`：

1. 给定 `s = "  SparkTech 2026  "`，依次 strip、collapse、upper 并每步 print  
2. 用切片取 `"2026"`  
3. 用 `join` 把 `["ST","2026","001"]` 拼成工号  

## 必做 B3 · 清洗工具增强

在课堂 `text_cleaner.py` 基础上增加：

- 选项 5：删除**所有**空白（F-05）  
- 清洗报告增加「减少了 N 个字符」  

## 必做 B4 · 改造 Day 1 卡片

复制 Day1 卡片为 `homework/card_with_clean.py`，对 `name`、`phone` 在赋值后调用 `strip_edges`；对 `phone` 可选删除所有空白。

## 必做 B5 · f-string Prompt

写 `homework/prompt_template.py`，用 f-string 生成一段不少于 100 字的「HR 问答」Prompt，含 `{context}` 与 `{question}` 两个占位变量。

## 选做 X1

读取 `sensitive_words.txt` 支持用户自定义词表路径。

## 挑战 C1

不用 `re`，仅用 `split`+`join` 实现 `collapse_spaces`。

---

提交：`homework/day02/你的姓名/`，commit `homework(day02): 姓名`
