# Day 30 课后作业（KEY DAY）

> 提交：`homework/day30/` · commit：`feat(day30): RAG CLI 作业`

## 必做 A · 评测集（35 分）

`eval_rag.py`：20 条 query，含 5 条应拒答。

指标：

- 有效问 **引用率** ≥ 80%  
- 拒答题 **准确率** ≥ 80%  

## 必做 B · 阈值调优（25 分）

扫描 `RAG_SCORE_THRESHOLD` ∈ {0.25, 0.35, 0.45}，输出误拒/误答表。

## 必做 C · Prompt 迭代（25 分）

修改 `my_rag_prompt.txt`，要求回答 **不超过 150 字**，对比原版长度。

## 必做 D · 客户脚本（15 分）

`demo_script.md`：5 分钟演示话术 + 3 个问答 + 异常处理。

## 挑战 E

接入 Day 24 FastAPI：`POST /api/rag/ask`。

---

**截止**：Day 31 上午 09:00
