# Day 6 课后作业

> 提交目录：`homework/day06/`  
> Git commit message 建议：`feat(day06): 函数作业 + 包重构扩展`

---

## 必做 A · 函数基础（40 分）

### A1. 实现 `format_employee_summary`

在 `homework/day06/func_basics.py` 中实现：

```python
def format_employee_summary(
    name: str,
    employee_id: str,
    *,
    department: str = "大模型应用开发部",
    show_dorm: bool = False,
    need_dorm: bool = False,
) -> str:
    """
    返回一行摘要。show_dorm 为 True 时追加「住宿：是/否」。
    department 必须用关键字参数传递（调用方练习 keyword-only 调用习惯）。
    """
```

验收：至少写 3 个调用示例（含关键字参数）。

### A2. `*args` 聚合

实现 `join_non_empty(*parts: str, sep: str = " | ") -> str`：跳过空字符串后用 `sep` 连接。

### A3. 口述题（写在 README 里）

用你自己的话解释：为什么 `def f(items=[])` 是 bug？正确写法是什么？

---

## 必做 B · 作用域与 lambda（30 分）

### B1. `make_tag_filter`

```python
def make_tag_filter(tag: str):
    """返回一个函数，接受 todo dict，若 tag 在其 tags 列表中则返回 True。"""
```

使用闭包，不要用 `global`。

### B2. 列表排序

给定 `todos: list[dict]`（含 `priority` 和 `id`），用 `lambda` 写一行 `sorted`，规则同 `sparktech_todo.list_todos(sort_by_priority=True)`。

### B3. 递归

实现 `count_char(text: str, ch: str) -> int`：**禁止**用 `str.count`，用递归统计 `ch` 在 `text` 中出现次数。`text` 为空时返回 0。

---

## 必做 C · 包重构验收（30 分）

1. 克隆/进入本仓库 `day06/code`，运行 `python3 refactor_verify.py`，截图四个 `[OK]`。  
2. 运行 `python3 run_export_json.py`，将 `output/employees_clean.json` 第一条记录贴入作业 README。  
3. 在 `homework/day06/notes.md` 画一张 **ASCII 目录树**（可抄讲义），标出四个包各自对应 Day 几的哪份脚本。

---

## 选做 D · 扩展 sparktech_todo

在 `homework/day06/` 新建 `todo_ext.py`：

- `import` 本日 `sparktech_todo.core`  
- 实现 `search_todos(todos, keyword: str) -> list[dict]`：标题包含 keyword（大小写不敏感）  
- 写 3 个 assert 自测  

---

## 选做 E · 清洗管道包装器

实现：

```python
def make_cleaner(**pipeline_flags):
    """根据 pipeline_flags 返回一个只接受 text 的一元函数。"""
    # 内部调用 sparktech_cleaners.apply_pipeline
```

示例：`fn = make_cleaner(do_strip=True, case_mode=CaseMode.UPPER)`，然后 `fn("  st-001  ")` → `"ST-001"`。

---

## 挑战 F · 递归深度展开

实现 `flatten_deep(nested: list) -> list`：任意深度嵌套列表展开为一维（仅含非 list 元素）。  

要求：
- 必须用递归  
- 附带 2 个测试用例，含 `[[1, [2, [3]]], 4]`  

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `func_basics.py` | A |
| `scope_lambda.py` | B |
| `notes.md` | C |
| `refactor_verify 截图` | C |
| `todo_ext.py` | D 选做 |
| `cleaner_wrapper.py` | E 选做 |
| `flatten_deep.py` | F 挑战 |

---

**截止时间**：Day 7 上午 09:00 前 push 到个人分支。
