# Day 19 课后作业

> 提交目录：`homework/day19/`  
> Git commit message 建议：`feat(day19): Function Calling Agent 作业`

---

## 必做 A · 新工具 schema（30 分）

### A1. 汇率转换工具

在 `homework/day19/schemas/exchange_rate.json` 编写 schema：

- 工具名：`convert_currency`
- 参数：`amount`（数字）、`from_currency`、`to_currency`（如 CNY、USD）

在 `homework/day19/tools_extra.py` 实现函数（mock 汇率表即可）：

```python
def convert_currency(amount: float, from_currency: str, to_currency: str) -> dict:
    ...
```

### A2. 注册到 Agent

修改作业版 `function_calling_agent.py`，加载新 schema 并注册到 `TOOL_REGISTRY`。

验收：用户问「100 美元等于多少人民币」能走 tool 循环。

---

## 必做 B · tool_runner 增强（25 分）

### B1. 超时保护

在 `homework/day19/tool_runner_safe.py` 为每个工具执行加 `@timeout(3.0)`（可复用 Day 13 装饰器）。

### B2. 错误消息格式

工具失败时，`tool` 消息的 `content` 必须是 JSON：

```json
{"error": true, "message": "除数不能为零"}
```

---

## 必做 C · Agent 轨迹导出（35 分）

### C1. 导出 JSON

扩展 `FunctionCallingAgent`，增加 `export_trace(path)` 将 `AgentTrace` 写入 JSON 文件。

### C2. 演示脚本

`homework/day19/run_demo.py` 依次演示：
1. 纯天气查询  
2. 纯计算  
3. 天气 + 计算组合  

### C3. 验收

```bash
python3 verify_day19.py   # 或作业目录等价脚本
```

截图含 `[OK] mock agent`。

---

## 选做 D · 并行 tool_calls

研究 OpenAI 一次返回多个 `tool_calls` 时，`tool_runner` 是否可 `asyncio.gather` 并行执行。  
在 `notes.md` 写 10 行结论。

---

## 选做 E · live 模式对比

有 Key 的同学对比 mock 与 live：
- 模型选工具是否更准确？
- 有无「该调不调」case？

---

## 挑战 F · ReAct 格式

实现简化 ReAct 输出解析：模型返回

```text
Thought: 需要查天气
Action: get_weather
Action Input: {"city": "北京"}
```

手写解析器转成 tool_call 结构（不依赖框架）。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `schemas/exchange_rate.json` | A |
| `tools_extra.py` | A |
| `tool_runner_safe.py` | B |
| `run_demo.py` + trace JSON | C |
| `notes.md` | C |
| `verify 截图` | C |
| `notes.md` 并行分析 | D 选做 |

---

**截止时间**：Day 20 上午 09:00 前 push 到个人分支。
