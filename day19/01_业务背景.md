# Day 19 业务背景 · 客服瞎编数据事故与工具调用 deadline

## 1. 事件回顾

**事件单号**：INC-AGENT-HALLUC-2026-0919  
**发起人**：产品王工 + 张工  
**关联**：Day 14 命令行助手、Day 20 相似问检索  
**严重程度**：P0（阻塞智能客服二期上线）  
**截止日期**：今日 17:30 提交可运行的 Function Calling Agent（mock 可验收）

### 1.1 现场情况

周四产品演示，王工的「星火智能客服」被客户当场质疑：

| 现象 | 客户原话 | 根因初判 |
|------|----------|----------|
| 天气瞎编 | 「北京明明下雨，它说晴天 35℃」 | 模型无实时数据，未接工具 |
| 算错账单 | 「`(99.5 * 12) - 优惠` 算成 1000」 | 心算不可靠，未调计算器 |
| 库存幻觉 | 「说有现货，仓库系统查无此 SKU」 | 未接产品库 API |
| 框架黑盒 | 「LangChain 报错看不懂堆栈」 | 团队未理解底层循环 |

张工在工单评论：

> 「Day 19 不讲框架，**手写 Function Calling 循环**。  
> 模型只负责 **决定调哪个工具、参数是什么**；真数据由你们的 Python 函数返回。  
> 三工具：**天气、计算器、产品库查询**。无 Key 必须 mock 跑通。」

### 1.2 与 Day 14 的对比

| 维度 | Day 14 CLI 助手 | Day 19 目标 |
|------|-----------------|-------------|
| 能力边界 | 纯文本对话 | 可调用外部工具 |
| API 参数 | 仅 `messages` | 增加 `tools` + `tool_choice` |
| 响应解析 | `content` 字符串 | 可能含 `tool_calls` 数组 |
| 多轮逻辑 | 单轮问答 | Agent 循环直至 `finish_reason=stop` |
| 框架依赖 | 无 | 仍保持无框架 |

## 2. 今日你在项目中的任务

```text
  上午  JSON Schema + tools 参数格式
        │
        ▼
  上午  tools.py 三工具实现
        │
        ▼
  下午  tool_runner 解析与执行
        │
        ▼
  下午  function_calling_agent 手写循环
        │
        ▼
  Day 20  Embedding 相似问 + RAG 检索
```

你被分配为 **Function Calling Agent** 负责人，干系人：

1. **张工**：验收 Agent 循环完整性（tool → result → 最终回答）  
2. **王工**：要求天气、计算、产品查询三场景 demo 可复现  
3. **CI 负责人**：无 Key 环境 `verify_day19.py` 全绿  

## 3. 与大模型开发的隐性联系

| 今日知识点 | 日后场景 |
|------------|----------|
| tools JSON Schema | OpenAI / 通义 / DeepSeek function calling |
| tool_call id 对应 | 并行多工具、结果回填 |
| AST 安全 eval | 代码执行沙箱、Calculator Tool |
| Agent while 循环 | LangChain AgentExecutor、AutoGPT |
| mock tool_calls | 单元测试、离线 CI |

张工补充：

> 「LangChain 的 `bind_tools`、OpenAI SDK 的 `client.chat.completions.create(tools=...)`，  
> 底层都是今天手写的这个循环。先手写，再引框架。」

## 4. 今日学习目标（业务语言）

1. 理解 Function Calling：模型输出结构化工具调用而非纯文本  
2. 会为业务函数编写 OpenAI 兼容 JSON Schema  
3. 实现 tool_runner：解析 arguments → 执行 → 生成 tool 消息  
4. 手写 Agent 循环，支持多轮 tool calling  
5. 无 API Key 时 mock 模式走通完整链路  

## 5. 验收口径（业务侧）

| 检查项 | 通过标准 |
|--------|----------|
| 天气查询 | 「北京天气」→ 调用 `get_weather` → 返回温湿度 |
| 数学计算 | 「(12+8)*3」→ 调用 `calculate` → 返回 60 |
| 产品检索 | 「查工单产品」→ 调用 `query_products` → 返回列表 |
| 多工具 | 一条用户消息触发多个 tool_call（mock 或 live） |
| 无 Key | `verify_day19.py` 8/8 通过 |

---

**讲师备注**：今日是 LLM 应用开发的 **分水岭**——从「聊天」升级为「能行动的 Agent」。
