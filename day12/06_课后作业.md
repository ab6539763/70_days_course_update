# Day 12 课后作业

> 提交目录：`homework/day12/`  
> Git commit message 建议：`feat(day12): HTTP 与 LLM API 作业`

---

## 必做 A · HTTP 与 requests（30 分）

### A1. 手写最小 GET

在 `homework/day12/mini_http.py` 中：

1. 用 `requests.get` 访问 `https://httpbin.org/uuid`  
2. 打印 `status_code` 与 `response.json()["uuid"]`  
3. 设置 `timeout=10`  

### A2. 对照 LLM 的 POST

在 `mini_http.py` 增加函数 `demo_llm_shaped_post()`：

- POST `https://httpbin.org/post`  
- body 含 `model`、`messages`（至少一条 user）  
- 打印状态码与回显的 `json` 字段  

验收：运行截图 + 在 `notes.md` 用一句话说明与 DeepSeek API 的相同点。

---

## 必做 B · LLMClient 扩展（35 分）

### B1. 复制并引用课程 `llm_client.py`

在 `homework/day12/` 中 `import` 课程 `LLMClient`（或复制后扩展），编写 `test_client.py`：

```python
def test_mock_mode():
    """无 Key 时 mock_mode 为 True，返回内容含 MOCK。"""

def test_build_messages():
    """system + user 两条，role 顺序正确。"""
```

可用 `assert` 或 `pytest`（Day 13 正式学）。

### B2. 自定义系统提示

用 `cli_chat_v0.py` 或自写脚本，对同一问题使用两种 `system_message`：

- 人设 A：严肃 IT 工单客服  
- 人设 B：活泼的同事助手  

各问一次：「公司年假有多少天？」（可 mock）  
在 `notes.md` 对比两条回复差异（各 2～3 句）。

---

## 必做 C · 环境与安全（35 分）

1. 创建 `homework/day12/.env.example`，变量名与课程一致。  
2. 本地 `.env` 填入真实 Key（**勿提交**）。  
3. 在 `notes.md` 写清：  
   - 新人如何从 clone 到跑出 `cli_chat_v0.py -q "你好"`（≤6 步）  
   - 若 Key 泄露应采取的 3 个应急步骤  

4. 运行 `python verify_day12.py` 截图三个 `[OK]`。

---

## 选做 D · 通义千问切换

阅读 [08_补充讲义_HTTP与LLM_API进阶.md](./08_补充讲义_HTTP与LLM_API进阶.md)，在 `homework/day12/qwen_client.py` 中：

- 复制 `LLMClient` 思路  
- 环境变量改为 `DASHSCOPE_API_KEY`（或课程补充讲义指定变量）  
- base_url 指向通义兼容端点  
- 保留 mock 模式  

---

## 选做 E · 错误处理增强

扩展 `LLMClient` 或包装函数：

- 捕获 `requests.Timeout`，返回友好中文提示  
- 对 HTTP 500 打印 `response.text` 前 200 字符  

在 `notes.md` 说明为何 401 不应自动重试。

---

## 挑战 F · 用量统计

在交互模式退出时，打印本次会话累计 `total_tokens`（LIVE 模式从 `ChatResponse.usage` 累加；mock 记 0）。  
提示：今日单轮每次一问一答，累加即可；Day 14 再优化。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `mini_http.py` | A |
| `test_client.py` | B |
| `notes.md` | A + B + C |
| `.env.example` | C |
| `verify_day12 截图` | C |
| `qwen_client.py` | D 选做 |
| `错误处理补丁` | E 选做 |
| token 统计版 CLI | F 挑战 |

---

**截止时间**：Day 13 上午 09:00 前 push 到个人分支。

**里程碑**：完成作业后可在 `notes.md` 末尾写一句「星火智服 First Token 感想」——李姐会抽读 🎉
