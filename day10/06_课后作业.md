# Day 10 课后作业

> 提交目录：`homework/day10/`  
> Git commit message 建议：`feat(day10): 模块包作业 + 异常与 venv 练习`

---

## 必做 A · import 与 `__name__`（30 分）

### A1. 模块运行对比

在 `homework/day10/import_lab.py` 中：

1. 定义 `def ping() -> str: return "pong"`  
2. 在文件末尾加 `if __name__ == "__main__":` 打印 `__name__` 和 `ping()`  
3. 另写 `homework/day10/import_lab_client.py`，`import import_lab` 并调用 `ping()`，**不**触发 main 块  

验收：两份脚本运行截图 + 口述 `__name__` 差异（写在 `notes.md`）。

### A2. 包导入练习

在 `import_lab_client.py` 中增加：

```python
from sparktech import validate_name, ValidationError
```

对空字符串调用并捕获 `ValidationError`，打印 `exc.field`。

---

## 必做 B · 异常处理（35 分）

### B1. safe_divide

在 `homework/day10/exceptions_lab.py` 实现：

```python
def safe_divide(a: float, b: float) -> float:
    """b 为 0 时 raise ValueError；返回 a/b。"""
```

写 `demo_safe_divide()` 用 try/except/else 演示成功与失败各一次。

### B2. 扩展 SparkTech 异常

在 `homework/day10/exceptions_lab.py` 增加：

```python
class RateLimitError(SparkTechError):
    """模拟 API 限流，附带 retry_after: int 秒。"""
```

构造一次并 `raise`，在 `main` 里捕获打印 `code` 与 `retry_after`。

### B3. finally 清理

实现 `read_lines_count(path)`：用 `try/finally` 保证文件句柄关闭，返回行数；文件不存在时 `raise DataLoadError`（可调用 `sparktech.load_json_file` 的错误风格自拟）。

---

## 必做 C · venv 与 requirements（35 分）

1. 在 `homework/day10/` 复制或引用课程 `requirements.txt`。  
2. 执行 `bash setup_venv.sh`（或在作业目录自建 venv），截图 `pip list` 含 `requests` 与 `python-dotenv`。  
3. 在 `notes.md` 用 **5 步以内** 写清：新人 clone 后如何创建环境并运行 `verify_package.py`。  

---

## 选做 D · 合并 Day 6 清洗函数

在 `homework/day10/sparktech_ext/` 扩展（或 fork 课程包）：

- 将 Day 6 `sparktech_cleaners.strip_edges` 迁入 `sparktech/utils/text.py`  
- 在 `sparktech/__init__.py` 导出  
- 更新 `verify_package.py` 或自写 assert  

---

## 选做 E · .env 实战

1. 在 `homework/day10/.env.example` 写 `SPARKTECH_API_KEY=your-key-here`  
2. 本地建 `.env`（不入库）  
3. 脚本调用 `get_env("SPARKTECH_API_KEY", required=True)` 并打印前 4 位 + `****`  

---

## 挑战 F · requirements 锁定

```bash
pip freeze > requirements.lock
```

在 `notes.md` 解释：`requirements.txt` 与 `requirements.lock` 在团队协作中分别承担什么角色。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `import_lab.py` | A |
| `import_lab_client.py` | A |
| `exceptions_lab.py` | B |
| `notes.md` | A + C |
| `pip list 截图` | C |
| `sparktech_ext/` | D 选做 |
| `.env.example` | E 选做 |
| `requirements.lock` + 说明 | F 挑战 |

---

**截止时间**：Day 11 上午 09:00 前 push 到个人分支。
