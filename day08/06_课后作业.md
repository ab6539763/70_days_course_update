# Day 8 课后作业 · OOP 基础与双类实战

> 请在今日 21:00 前提交至 `homework/day08/你的姓名/`。重点验收 **类设计** 与 **Day 7 互操作**，不要求完整 CLI。

## 必做 B1 · 概念速答（书面）

1. 类和对象分别是什么？各举一个今日代码中的例子。  
2. `self` 是什么？为什么实例方法的第一个参数必须写 `self`？  
3. `@classmethod` 和 `@staticmethod` 各适合什么场景？各举今日代码一例。  
4. `__init__` 和 `__str__` 分别在什么时候被调用？

## 必做 B2 · 运行四个 demo

```bash
cd day08/code
python3 oop_demo.py
python3 contact_class.py
python3 classmethod_demo.py
python3 chat_message.py
```

截图或粘贴各程序最后 5 行输出，并用自己的话说明每个文件演示了什么。

## 必做 B3 · Contact 互操作

在 `homework/day08/contact_lab.py` 中：

1. 从 `day07/code/contacts.json` 读取第一条联系人（若路径不存在则用 PRD 示例 dict）。  
2. 用 `Contact.from_dict` 转为对象并 `print(contact)`。  
3. 调用 `contact.update(department="产品部")` 后 `print(contact.to_dict()["department"])`，应输出 `产品部`。

可 `from contact_class import Contact`（注意 `sys.path` 或相对复制类定义，讲师接受任一方式）。

## 必做 B4 · ChatMessage 三轮对话

在 `homework/day08/dialog_demo.py` 中构造：

```python
messages = [
    ChatMessage("system", "你是星火智服内部助手，回答简洁专业。"),
    ChatMessage("user", "通讯录存在哪？"),
    ChatMessage("assistant", "Week1 在 contacts.json，Day8 起用 Contact 类管理。"),
]
```

遍历 `messages` 打印 `str(m)`；再 `json.dumps([m.to_dict() for m in messages], ensure_ascii=False, indent=2)` 写入 `dialog_sample.json`。

## 必做 B5 · 手绘或 Mermaid 类图

为 `Contact` 与 `ChatMessage` 画类图（属性 + 方法），标注哪些是实例方法、`@classmethod`、`@staticmethod`。

## 必做 B6 · Git 提交

```bash
git add homework/day08/
git commit -m "homework(day08): 姓名 - Contact与ChatMessage练习"
git push
```

## 选做 X1 · 轻量 ContactBook

实现 `ContactBook` 类（可参考 `contact_class.py` 骨架）：

- `add(name, phone, **kwargs)`  
- `find_by_id(id)`  
- `search(keyword)` 在姓名/手机/部门中子串匹配  
- `save(path)` / `load(path)` 读写与 Day 7 相同 JSON 格式  

不要求完整菜单，写 3 个 `assert` 自测即可。

## 选做 X2 · ChatMessage 扩展

为 `ChatMessage` 增加：

- `word_count()` 实例方法：返回 content 按空白分词后的词数（中文按字符数亦可，注释说明规则）。  
- `merge_system_prompt(messages)` classmethod：从列表中提取所有 `system` 消息 content 并用 `\n` 拼接。

## 挑战 C1 · 对比测试

写 `homework/day08/compare_dict_vs_class.py`：

- 用 Day 7 风格 `dict` 与 Day 8 `Contact` 各实现「修改手机并检查 updated_at 变化」。  
- 注释说明哪种写法更不易写错键名。

## 挑战 C2 · Day 14 预习骨架

创建 `homework/day08/mini_repl.py`：

- `messages: list[ChatMessage] = []`  
- `while True` 读用户输入，`/exit` 退出，`/history` 打印全部 `str(msg)`，普通输入 append `ChatMessage("user", text)` 并 append 一条假的 `ChatMessage("assistant", "【占位】Day14 接 API")`  

不要求调真实模型。

---

提交路径：`homework/day08/你的姓名/`

讲师复查：周二早会抽查 3 人解释 `from_dict` 与 `normalize_phone` 为何一个是 classmethod、一个是 staticmethod。
