# Day 41 课后作业

## 必做

**B1** 在 `langgraph_react.py` 增加 `max_steps` 从环境变量读取。

**B2** `approval_node_demo.py` 增加 `interrupt` 注释说明如何接 FastAPI 审批 API。

**B3** 用 `app.get_graph().draw_mermaid_png()` 导出 PNG（若依赖齐全）。

## 选做

**X1** 合并 ReAct 图 + 审批图：route 前插入 approval 子图。

## 挑战

**C1** 用 LangGraph `MemorySaver` + `thread_id` 演示审批暂停与恢复。

---

commit 含 `day41`
