# Day 41 作业参考答案

## B1 · max_steps

```python
MAX_STEPS = int(os.getenv("REACT_MAX_STEPS", "6"))

def should_continue(state):
    if state.get("step", 0) >= MAX_STEPS:
        return "end"
```

## B2 · interrupt 说明

```python
app = graph.compile(
    checkpointer=MemorySaver(),
    interrupt_before=["approval"],
)
# 第一次 invoke 在 approval 前暂停
# 人工批准后：app.invoke(None, config={"configurable": {"thread_id": "t1"}})
```

## X1 · 子图思路

将 `build_approval_graph` 编译为 subgraph，`add_node("approval_flow", subgraph)`。

---

*Day 41 参考答案*
