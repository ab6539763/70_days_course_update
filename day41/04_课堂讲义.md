# Day 41 课堂讲义 · LangGraph 入门

## 第一章 · StateGraph 基础（09:00–10:00）

```python
from typing import TypedDict
from langgraph.graph import StateGraph, END

class MyState(TypedDict):
    count: int

def increment(state: MyState):
    return {"count": state["count"] + 1}

g = StateGraph(MyState)
g.add_node("inc", increment)
g.set_entry_point("inc")
g.add_edge("inc", END)
app = g.compile()
app.invoke({"count": 0})
```

## 第二章 · ReAct 图（10:00–12:00）

[langgraph_react.py](./code/langgraph_react.py)：

```python
graph.add_node("reason", reason_node)
graph.add_node("act", act_node)
graph.set_entry_point("reason")
graph.add_conditional_edges("reason", should_continue, {"act": "act", "end": END})
graph.add_edge("act", "reason")
```

对比 [Day 39](../day39/code/react_agent.py) 的 `for` 循环——**图即代码**。

## 第三章 · 可视化（14:00–15:00）

```bash
python3 graph_visualize.py
# 输出 output/react_graph.mmd
```

## 第四章 · 审批节点（15:00–17:00）

[approval_node_demo.py](./code/approval_node_demo.py)：

- `after_classify`：complaint/refund → approval  
- `after_approval`：approved → route else reject  

生产用 `interrupt_before=["approval"]` + Checkpointer 实现真 HITL。

```bash
python3 verify_day41.py
```

## 小结

Phase3 Agent 三周课收官：**手写 → 框架 → 图编排**。下周接真实工单队列。
