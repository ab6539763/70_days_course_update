# Day 62 附录 · 测试与护栏接入指南

> Day 62：**扩展 `tests/verify_project.py` + 可选接入 Day46 护栏**。

---

## 第 1 节 · verify 扩展模板

在 `workspace/<name>/tests/verify_project.py`：

```python
def test_health():
    assert True  # 替换为真实 health 检查

def test_main_flow_mock():
    # SPARKTECH_MOCK=1 下跑通主路径
    ...

def main():
    test_health()
    test_main_flow_mock()
    print("[OK] graduation verify")
```

---

## 第 2 节 · 护栏接入（加分）

复制 `day46/code/agent_guardrails.py` 概念：

| 层 | 接入点 |
|----|--------|
| InputGuard | API 入口 |
| OutputGuard | 返回用户前 |
| max_rounds | Agent 循环 |

---

## 第 3 节 · CI 建议

```yaml
# .github/workflows/verify.yml 选做
- run: cd workspace/my-project && python3 tests/verify_project.py
  env:
    SPARKTECH_MOCK: "1"
```

---

*附录 · Day 62*
