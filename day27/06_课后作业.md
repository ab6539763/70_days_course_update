# Day 27 课后作业

> 提交目录：`homework/day27/`

---

## 必做 A · 验收（25 分）

`python3 code/verify_day27.py` 全绿 → `verify_ok.png`

---

## 必做 B · 三轮记忆实验（35 分）

在 `homework/day27/memory_notes.md` 记录：

1. 同一 `session_id` 连问 3 轮后 `/history` 条数  
2. 切换 `/session other` 后再问，条数变化  
3. `/mode window` 后连问 5 轮，最早消息是否被裁掉  

---

## 必做 C · 对接 Day 14 JSON（40 分）

编写 `homework/day27/export_history.py`：

1. 从 `RunnableWithMessageHistory` 的 history 导出为 Day 14 兼容 JSON  
2. 字段含 `session_id`, `messages[{role, content}]`  
3. 与 [Day 14 data/sessions](../day14/data/sessions/) 格式对照说明  

---

## 选做 D · Day 24 迁移草案（10 分）

在 `homework/day27/web_memory_plan.md` 描述如何把 [Day 24 chat.py](../day24/backend/chat.py) 改为使用 `RunnableWithMessageHistory`（不需真改 day24 代码）。

---

## 提交清单

- [ ] `verify_ok.png`  
- [ ] `memory_notes.md`  
- [ ] `export_history.py`  
- [ ] （选做）`web_memory_plan.md`
