# Day 27 流程图与示意图

## 1. RunnableWithMessageHistory 时序

```mermaid
sequenceDiagram
    participant U as 用户
    participant C as chain_with_history
    participant H as get_session_history
    participant L as LCEL core

    U->>C: invoke(user_input, session_id)
    C->>H: get_session_history(session_id)
    H-->>C: ChatMessageHistory
    C->>C: 读取 history 注入输入
    C->>L: invoke({history, user_input})
    L-->>C: reply
    C->>H: add_user + add_ai
    C-->>U: reply
```

## 2. Memory 模式切换

```text
SPARKTECH_MEMORY_MODE
        │
   ┌────┼────┬────────┐
   ▼    ▼    ▼        ▼
memory window summary sqlite
   │    │    │        │
   └────┴────┴────────┴──► ChatMessageHistory 子类 / SQL 实现
```

## 3. Phase2 技术栈总览

```mermaid
flowchart TB
    D25[Day25 ChatModel+Prompt]
    D26[Day26 LCEL+Parser]
    D27[Day27 Memory]
    D14[Day14 CLI 原型]
    D24[Day24 Web SSE]

    D14 --> D25 --> D26 --> D27
    D24 -.->|后续统一| D27
```

## 4. Session 隔离

```text
session-a ──► History A (独立 messages)
session-b ──► History B (独立 messages)
```

`configurable.session_id` 是隔离键。
