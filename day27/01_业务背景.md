# Day 27 业务背景 · Phase2 Memory 层接入

## 1. 事件回顾

**事件单号**：PHASE2-MEMORY-2026-0727  
**关联**：[Day 25 CLI](../day25/)、[Day 24 Web](../day24/)、[Day 14 会话](../day14/)

Day 25 迁移后，学员手动维护 `LangChainSession.history` 传入 `MessagesPlaceholder`。架构组评审指出：

| 问题 | 风险 |
|------|------|
| 历史裁剪逻辑分散 | token 超限 |
| 会话 ID 与存储未统一 | Web/CLI 不一致 |
| 无摘要策略 | 长对话丢上下文 |

张工要求：

> 「用 `RunnableWithMessageHistory` 包装 Day 26 的 LCEL 链；提供 memory / window / sqlite 三种后端，CLI 用 `/mode` 切换演示。」

## 2. 与 Day 24 Web 的关系

[Day 24](../day24/) 已在 FastAPI + SQLAlchemy 中持久化消息。Day 27 **不修改** Day 24 代码，但概念对齐：

| Day 24 | Day 27 |
|--------|--------|
| `ChatSession` + `ChatMessage` ORM | `SQLChatMessageHistory` |
| `session_id` 请求字段 | `configurable.session_id` |
| 刷新加载历史 | History 工厂重载 |

Phase2 后期将把 Web 后端 history 读写改为 LangChain Memory 接口。

## 3. 今日任务路径

```text
  ChatMessageHistory（内存）
        │
        ▼
  RunnableWithMessageHistory
        │
        ├── WindowChatMessageHistory
        ├── SummaryChatMessageHistory
        └── SQLChatMessageHistory
```

## 4. 成功标准

- CLI 多轮对话自动累积 history  
- 不同 `session_id` 互不干扰  
- `verify_day27.py` 无 Key 全绿  
