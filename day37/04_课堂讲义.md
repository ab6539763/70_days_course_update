# Day 37 课堂讲义 · 前端与全栈联调

> **讲师**：张工 · 预计 3.5 小时

---

## 第 1 节 · 页面结构（40 min）

打开 `frontend/index.html`：

- `aside.sidebar` — 上传 + 文档列表  
- `main.chat-main` — 对话区  

对比 Day 24 单栏布局，讲解 **双栏企业后台** 常见模式。

CSS 变量在 `style.css` `:root`，学员可改 `--accent` 做品牌色。

---

## 第 2 节 · 引用徽章组件（50 min）

### 2.1 buildCitationBadges

```javascript
function buildCitationBadges(citations) {
  const wrap = document.createElement('div');
  wrap.className = 'citations';
  citations.forEach((cite, idx) => {
    const badge = document.createElement('span');
    badge.className = 'citation-badge';
    badge.title = cite.snippet;
    // [1] source
  });
  return wrap;
}
```

### 2.2 与流式结合

关键：**citations 事件到达时** 即渲染徽章，不必等全文完成。

```javascript
await fetchChatStream(text, onCitations, onDelta);
```

### 2.3 上机

修改徽章点击为自定义 modal（选做），默认 `alert` 即可。

---

## 第 3 节 · SSE 解析复习（30 min）

复用 Day 24 `parseSSEBuffer`，强调：

- 不用 `EventSource`（因为 POST）  
- buffer 拼接防断行  

新增事件类型：

```json
{"event":"citations","citations":[...]}
```

---

## 第 4 节 · 文档上传（40 min）

```javascript
const form = new FormData();
for (const f of pendingFiles) {
  form.append('files', f);
}
await fetch(`${API_BASE}/api/documents/upload`, { method: 'POST', body: form });
```

注意：multipart **不要** 手动设 Content-Type。

上传成功后 `loadDocuments()` + `checkHealth()`。

---

## 第 5 节 · 历史与 session（30 min）

`loadHistory` 对 assistant 消息传入 `msg.citations`：

```javascript
appendMessage(msg.role, msg.content, msg.citations || []);
```

`session_id` 键名改为 `sparktech_kb_session_id` 避免与 Day 24 冲突。

---

## 第 6 节 · run.sh 联调彩排（40 min）

```bash
cd day37
bash run.sh
```

Demo checklist 见 `01_业务背景.md`。

```bash
python3 verify_project2.py
```

---

## 课堂 CHECKLIST

- [ ] 全员 run.sh 可访问 8080  
- [ ] 流式 + 引用同时可见  
- [ ] verify 全绿  

---

*主课 · Day 37*
