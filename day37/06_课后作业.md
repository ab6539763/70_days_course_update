# Day 37 课后作业

## 作业 A · 必做（联调）

### A1. 完整 Demo 录屏（20 分）

录制 3–5 分钟屏幕视频，包含：

- run.sh 启动  
- 上传文档  
- 流式问答 + 引用点击  
- 刷新保留历史  

### A2. 引用 UI 优化（15 分）

将 `alert` 改为页面内 **tooltip 或侧边抽屉** 展示完整 snippet 与 score。  
保持移动端可读（响应式已有基础）。

### A3. 集成测试日志（15 分）

运行 `verify_project2.py`，将完整终端输出粘贴到 `integration_log.txt` 并提交。

---

## 作业 B · 选做

### B1. 多会话列表（+10 分）

侧边栏增加历史 session 列表（需扩展 API 或 localStorage 多 id）。

### B2. 打字机光标优化（+5 分）

流式结束后移除 `streaming` 光标动画，失败时保留错误样式。

### B3. E2E Playwright（+15 分）

编写 1 个 Playwright 用例：打开页面 → 发送问题 → 等待 `.citation-badge`。

---

## 提交要求

- Commit message 含 `day37`  
- 分支 `day37-homework-姓名`  

---

*课后作业 · Day 37*
