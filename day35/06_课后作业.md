# Day 35 课后作业

## 作业说明

- 完成后提交至 `homework/day35/`  
- Git commit message 含 `day35-homework`  
- 必做预计 2.5h，选做 1.5h，挑战 2h+

---

## 必做（100 分）

### B1 · 跑通验收（20 分）

```bash
cd day35/code && python3 verify_day35.py
```

截图「7/7 通过」。

### B2 · 框架对照笔记（25 分）

阅读 `compare_frameworks.md`，补充表格：**你最熟悉的 3 个 RAG 步骤** 在 LangChain 与 LlamaIndex 中各对应什么类/函数？提交 `homework/day35/framework_notes.md`。

### B3 · 问答演示（25 分）

用 REPL 模式回答以下 3 题并截图：

```bash
python3 llamaindex_kb.py --repl --force-mock
```

1. 如何申请退款？  
2. 忘记密码怎么办？  
3. 星火智服支持哪些大模型？  

### B4 · 概念题（30 分）

1. LlamaIndex 的 QueryEngine 封装了哪两步？  
2. 为什么今日提供 `MockKBEngine`？  
3. Day 35 与 Day 33 使用同一 `kb_docs` 的意义是什么？  

---

## 选做（+20 分）

### X1 · LlamaIndex live 模式

安装 `llama-index` 与 embedding 包，配置 Key，对比 mock 与 live 检索 Top-3 是否一致。

### X2 · 评估横向对比

将 Day 35 的 3 条问答结果送入 Day 34 `evaluate_rag_sample`，与 Day 33（若有）分数制表。

---

## 挑战（+30 分）

### C1 · 框架选型一页纸

写 `homework/day35/selection_memo.md`（≤500 字）：若星火智服 **只做 KB 问答**，选 LangChain 还是 LlamaIndex？若 **要做多 Agent**，又如何组合？

### C2 · 引用溯源增强

修改 `llamaindex_kb.py`，在答案末尾列出所有 chunk 的 `source` 与 `score` 表格（Markdown 格式）。

---

**提交清单**：验收截图 + REPL 截图 + framework_notes.md + 概念题答案
