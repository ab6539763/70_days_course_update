# Day 35 作业参考答案

## B1 · 验收

期望：

```text
验收结果: 7/7 通过
```

---

## B2 · framework_notes.md（参考）

| RAG 步骤 | LangChain (Day 33) | LlamaIndex (Day 35) |
|----------|-------------------|---------------------|
| 加载文档 | `DirectoryLoader` | `SimpleDirectoryReader` |
| 文本切块 | `RecursiveCharacterTextSplitter` | `SentenceSplitter` |
| 向量索引 | `Chroma.from_documents` | `VectorStoreIndex.from_documents` |
| 检索 | `vectorstore.as_retriever(k=3)` | `index.as_retriever(similarity_top_k=3)` |
| 问答 | `create_retrieval_chain` | `index.as_query_engine()` |

---

## B4 · 概念题

1. **QueryEngine** 封装 **检索（retrieve）** 与 **答案合成（synthesize）**。  
2. **MockKBEngine** 保证无 `llama-index`、无 API Key 时课堂与 CI 仍可演示完整问答链路。  
3. **同一 kb_docs** 才能公平对比框架差异，排除文档内容变量，评估分数才有可比性。

---

## X2 · 评估横向对比（表头参考）

| 问题 | 框架 | F | R | CP | CR |
|------|------|---|---|----|----|
| 如何申请退款？ | LangChain | 0.75 | 0.15 | 0.6 | 1.0 |
| 如何申请退款？ | LlamaIndex | 0.79 | 0.12 | 0.5 | 1.0 |

（数值为示例，以你本地跑分为准）

---

## C1 · selection_memo.md（参考摘要）

> **纯 KB 问答**：优先 LlamaIndex，索引与 QueryEngine 路径短、连接器丰富。  
> **多 Agent 办公**：LangChain + LangGraph 负责工具环与状态机；RAG 子模块可用 LlamaIndex 作独立索引服务。  
> 星火智服采用 **双栈**：索引层 LlamaIndex，Agent 层 LangChain，评估统一 Day 34。

---

## C2 · 溯源表格（实现提示）

在 `format_result` 末尾追加：

```python
lines.append("\n| # | source | score |")
for i, c in enumerate(result.chunks, 1):
    lines.append(f"| {i} | {c.source} | {c.score:.4f} |")
```

---

## 讲评要点

- mock 检索分数绝对值低正常，看 **相对排序** 是否把正确 chunk 排前  
- live 模式注意 embedding 模型与 Day 33 一致才可比  
- 框架选型写清 **场景**，避免教条式「谁更好」
