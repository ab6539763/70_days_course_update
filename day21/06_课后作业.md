# Day 21 课后作业

> 提交目录：`homework/day21/`  
> Git commit message 建议：`feat(day21): week3 review and integrated assistant`

---

## 必做 A · 测验订正（30 分）

1. 对照 [07_作业参考答案.md](./07_作业参考答案.md) 订正 `week3_review_quiz.md` 全部错题。  
2. 在 `homework/day21/quiz_corrections.md` 写 **每道错题** 的：
   - 原错误答案  
   - 正确答案  
   - 一句话理由  

---

## 必做 B · 综合助手验收（40 分）

### B1. 跑通验收

```bash
cd day21/code
python3 verify_day21.py
```

截图含全部 `[OK]`，保存为 `homework/day21/verify_screenshot.png`（或粘贴终端输出到 `verify_log.txt`）。

### B2. 三轮演示记录

在 `homework/day21/demo_transcript.txt` 记录以下三轮对话的 **完整终端输出**：

1. 多轮：自我介绍 + 追问名字  
2. 工具：查询任意城市天气  
3. 流式：`/stream on` 后任意一问  

---

## 必做 C · 工具扩展（30 分）

在 `homework/day21/my_tool.py` 为 `ToolRegistry` 增加第四个工具 `get_time`：

- 触发关键词：`时间`、`几点`  
- 返回：当前 UTC 时间 ISO 格式字符串  
- 在 `notes.md` 说明如何注册到 `integrated_assistant.py`  

验收：运行扩展后的助手，问「现在几点」能触发新工具。

---

## 选做 D · live 模式

配置真实 `OPENAI_API_KEY`，在 `notes.md` 记录：

- mock vs live 工具触发差异  
- 一次失败请求与错误提示截图  

---

## 选做 E · pytest

延续 Day 15，在 `homework/day21/test_session.py` 写 3 个用例：

```python
def test_trim_keeps_system():
    ...

def test_tool_weather():
    ...

def test_stream_tokens_nonempty():
    ...
```

---

## 选做 F · 为 Day 22 准备 Mock 数据

导出 `homework/day21/ui_mock_messages.json`：

```json
[
  {"role": "user", "content": "查上海天气"},
  {"role": "assistant", "content": "上海当前天气：晴，26°C..."}
]
```

至少 4 条消息，供前端 Lisa 导入静态 Demo。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `quiz_corrections.md` | A |
| `verify_screenshot` 或 `verify_log.txt` | B |
| `demo_transcript.txt` | B |
| `my_tool.py` + `notes.md` | C |
| `test_session.py` | E 选做 |
| `ui_mock_messages.json` | F 选做 |

---

**截止时间**：Day 22 上午 09:00 前 push 到个人分支。
