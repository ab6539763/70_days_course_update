# Day 21 业务背景 · Week 3 结业考与综合助手验收

## 1. 事件回顾

**事件单号**：MIL-W3-REVIEW-2026-0721  
**发起人**：张工 + QA 小陈  
**关联**：Day 15–20 进阶专题、Day 14 Stage Project 1  
**严重程度**：P0（阻塞 Week 4 前端联调排期）  
**截止日期**：今日 17:00 提交可运行的 `integrated_assistant.py` + 测验答卷

### 1.1 现场情况

周四晚，星火智服 Sprint 3 评审会上，产品提出三个问题：

| 问题 | 现状 | 期望 |
|------|------|------|
| 多轮对话是否丢上下文？ | Day 14 CLI 可用，但未测长会话 | 20+ 轮仍稳定 |
| 工具调用是否可用？ | Day 19 单测通过，未接入主流程 | 天气/订单查询走工具环 |
| 流式体验如何？ | Day 20 SSE 在 HTTP 层，CLI 无流式 | 终端可逐字输出 |

张工在工单评论：

> 「Week 3 不是再加一个新 demo，是 **结业考**：  
> 上午做 `week3_review_quiz` 检验概念；下午把 Day 15–20 的能力 **焊进一个入口**。  
> 设计部 Day 22 要做静态 Chat 原型，你们今天交付的交互契约就是他们的参考。」

### 1.2 Week 3 知识地图（业务视角）

```text
  Day 15  多厂商 LLM + pytest 质量门禁
      │
  Day 16  temperature / max_tokens 调参实验
      │
  Day 17  Prompt 四要素与模板工程
      │
  Day 18  JSON Mode + Rich 终端增强
      │
  Day 19  Function Calling / 工具注册
      │
  Day 20  FastAPI + httpx 流式 SSE
      │
      ▼
  Day 21  ★ 综合助手 integrated_assistant.py
```

## 2. 今日你在项目中的任务

```text
  上午  week3_review_quiz 闭卷/开卷测验
        │
        ▼
  上午  讲评薄弱点（多轮截断、tool schema、yield 流式）
        │
        ▼
  下午  实现 integrated_assistant.py
        │  ├─ ConversationSession
        │  ├─ ToolRegistry + 工具执行环
        │  └─ stream_chat 流式输出
        │
        ▼
  傍晚  verify_day21 + mock 演示录屏
        │
        ▼
  Day 22  前端静态 Chat UI（设计部客户 Demo）
```

干系人：

1. **张工**：验收工具环不超过 3 轮递归、流式可开关  
2. **QA 小陈**：无 Key 环境 `verify_day21.py` 全绿  
3. **设计部 Lisa**：需要 CLI 交互示例截图作为 UI 文案参考  
4. **Day 24 联调负责人**：需要 `/tools` 输出与 HTTP 契约一致  

## 3. 与大模型开发的隐性联系

| 今日知识点 | 日后场景 |
|------------|----------|
| 多轮 `messages` 截断 | 控制 token 成本、防上下文溢出 |
| Tool schema 与执行环 | Agent、RAG 检索、工单系统对接 |
| 流式 `yield` | SSE、WebSocket、打字机 UX |
| mock 优先闭环 | CI、答辩、客户 Demo 不依赖外网 |
| 综合编排 | LangGraph / 自研 Agent 内核雏形 |

张工补充：

> 「OpenAI Assistants API、LangChain Agent，底层都是今天这个模式：  
> **messages 历史 + tools 定义 + 流式 delta**。先手写一遍，再引框架。」

## 4. 今日学习目标（业务语言）

1. 通过 Week 3 测验，巩固 Day 15–20 核心概念  
2. 实现可运行的综合助手，整合多轮、工具、流式  
3. 理解工具调用环：模型请求 tool → 执行 → 结果回注 → 再生成  
4. 能向非技术同事演示 mock 模式下的完整对话流程  
5. 为 Day 22 静态 UI 提供交互与文案参考  

## 5. 风险与约束

| 风险 | 缓解 |
|------|------|
| 学员 Day 15–20 笔记不齐 | 提供 `08_补充讲义_Week3知识图谱.md` |
| 真实 API 限流影响课堂 | 默认 mock，`SPARKTECH_MOCK=0` 才走 live |
| 工具环死循环 | `MAX_TOOL_ROUNDS=3` 硬限制 |
| 流式与 Rich 冲突 | 今日用 `print(..., end="", flush=True)` 即可 |

---

**一句话**：Week 3 结业考——证明你不仅会调 API，还能 **编排** 一个像样的大模型应用内核。
