# Day 21 · 复制为 .env 后填入（.env 不入库）
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

# 1/true = 强制 mock（课堂默认）；0 = 有 Key 时走 live
SPARKTECH_MOCK=1

# 默认流式：on / off
STREAM_DEFAULT=on
