# Day 36 课堂讲义 · 后端实现主课

> **讲师**：张工 · 预计 3.5 小时（含上机）

---

## 第 1 节 · 项目初始化（30 min）

### 1.1 目录结构

```bash
cd day36/code/project2
tree -L 2
```

强调 **backend 包** 与 `PYTHONPATH`：

```bash
export PYTHONPATH=$PWD
uvicorn backend.main:app --reload --port 8000
```

### 1.2 数据模型速览

打开 `backend/models.py`，讲解四张表：

- `ChatSession` / `ChatMessage` — 继承 Day 24  
- `DocumentRecord` / `KnowledgeChunk` — Project 2 新增  

**课堂提问**：为什么 citations 存在 message 行而不是单独表？  
**参考答案**：教学规模下 JSON 快照足够；生产可拆 `message_citations` 关联表。

---

## 第 2 节 · 文档摄取 document_ingest.py（45 min）

### 2.1 多格式解析

```python
# txt / md → read_text
# pdf → pypdf.PdfReader
```

演示上传 `refund_policy.md`，观察 `data/uploads/` 与 SQLite。

### 2.2 切块策略

```python
chunk_text(text, chunk_size=380, overlap=70)
```

对比 Day 28 固定窗口与 Day 33 RecursiveCharacterTextSplitter：

- 教学项目用轻量 `chunk_text`  
- 生产建议按标题 + 递归切分  

### 2.3 上机任务

1. 在 `data/sample_docs/` 新增 `shipping_policy.txt`  
2. 重启 API，确认 lifespan 自动 ingest  
3. `GET /health` 查看 `chunk_count` 增加  

---

## 第 3 节 · Hybrid 检索（45 min）

### 3.1 BM25 + Vector

复习 Day 33 `HybridRetriever`：

```python
hits = retriever.retrieve(query, top_k=5)
reranked = rerank(query, hits, top_k=3)
```

### 3.2 引用对象

```python
CitationOut(
    chunk_id=hit.chunk_id,
    source=hit.source,
    snippet=make_snippet(hit.content),
    score=hit.rrf_score,
)
```

**强调**：`source` 必须是用户可识别的 **文件名**，不是内部 UUID。

### 3.3 上机任务

在 Python REPL 中：

```python
from backend.database import SessionLocal, init_db
from backend.rag_service import get_rag_service
init_db()
db = SessionLocal()
rag = get_rag_service()
rag.load_index(db)
print(rag.retrieve(db, "退款 7 天"))
```

---

## 第 4 节 · rag_service 与 mock 生成（40 min）

### 4.1 拒答逻辑

无 citations 时返回：

```text
抱歉，我在当前知识库中没有找到与您问题相关的内容...
```

### 4.2 mock_generate 模板

展示如何拼接多条引用 + 标注最高分来源。

### 4.3 stream_answer 协议

```python
yield ("citations", citations, None)
yield ("delta", None, chunk)
yield ("done", citations, None)
```

---

## 第 5 节 · FastAPI 路由 main.py（40 min）

### 5.1 上传接口

```python
@app.post("/api/documents/upload")
async def upload_documents(files: list[UploadFile] = File(...), ...):
```

上传后 **必须** `rag.load_index(db)` 刷新内存索引。

### 5.2 SSE 流式

对照 Day 24 `chat_stream`，新增 `citations` 事件分支。

### 5.3 上机验收

```bash
python3 verify_project2.py
```

---

## 第 6 节 · 答疑与明日预告（10 min）

Day 37 将：

- 完善 `frontend/app.js` 引用徽章  
- 文件上传 UI 联调  
- `bash run.sh` 全栈启动  

---

## 课堂 CHECKLIST

- [ ] 学员能画出 ingest → index → retrieve 数据流  
- [ ] 学员能解释 citations 事件为何在 delta 之前  
- [ ] 全员 verify 通过或记录失败项  

---

*主课讲义 · Day 36*
