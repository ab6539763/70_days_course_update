# Day 36 作业参考答案

## A1. PDF 样本

可使用任意 PDF 生成工具，示例内容：

```text
企业版定价：标准价 12 万/年起，含 100 并发与 SLA 99.9%。
```

验证命令：

```bash
cd day36/code/project2
python3 -c "
from backend.database import SessionLocal, init_db
from backend.document_ingest import ingest_directory
init_db()
db = SessionLocal()
ingest_directory(db)
print('done')
"
```

## A2. page 字段（示例片段）

`schemas.py`：

```python
class CitationOut(BaseModel):
    ...
    page: int | None = None
```

`document_ingest.py` PDF 切块时可传 `page_num` 到 chunk metadata，检索时透传。

## A3. 拒答阈值

`rag_service.py`：

```python
def retrieve(self, db: Session, query: str, top_k: int = 3) -> list[CitationOut]:
    ...
    reranked = rerank(query, hits, top_k=top_k)
    if reranked and reranked[0].rrf_score < 0.15:
        return []
    ...
```

测试：

```bash
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"今天比特币价格","session_id":"hw","stream":false}'
```

应返回拒答文案且 `citations: []`。

## B1. live 模式

`.env`：

```env
SPARKTECH_MOCK=0
OPENAI_API_KEY=sk-...
OPENAI_CHAT_MODEL=gpt-4o-mini
```

## B2. pytest 示例

```python
def test_hybrid_empty():
    from backend.hybrid_retriever import HybridRetriever
    r = HybridRetriever([])
    assert r.retrieve("test") == []
```

---

*参考答案 · 讲师内部使用，选择性讲解*
