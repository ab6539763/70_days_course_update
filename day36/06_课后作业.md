# Day 36 课后作业

## 作业 A · 必做（后端巩固）

### A1. 补充 PDF 样本（15 分）

1. 在 `data/sample_docs/` 添加一份 **自制 PDF**（可用 Word/LibreOffice 导出），内容含「企业版定价」段落。  
2. 确认 `verify_project2.py` 通过或自行编写 PDF ingest 测试。  
3. 截图 `GET /api/documents` 显示该文件。

### A2. citations 字段扩展（15 分）

在 `CitationOut` 中新增可选字段 `page: int | None`（PDF 页码，txt/md 为 null）。  
修改 `document_ingest.py` 在 PDF 切块时写入 metadata（教学可只记录首块页码）。  
提交 diff 说明。

### A3. 拒答阈值（10 分）

在 `rag_service.retrieve` 中：若 rerank 后 top1 `score < 0.15`，返回空 citations 触发拒答。  
用无关问题「今天比特币价格」验证。

---

## 作业 B · 选做

### B1. live 模式联通（+10 分）

配置 `.env` 中 `OPENAI_API_KEY`，`SPARKTECH_MOCK=0`，完成一次真实问答并截图。

### B2. 单元测试（+10 分）

用 `pytest` 为 `hybrid_retriever.retrieve` 写 ≥2 个用例（含空库）。

### B3. 中文 PDF 扫描件说明（+5 分）

写 200 字说明：为何本课 pypdf 不适合扫描版 PDF，生产应如何处理（OCR）。

---

## 提交要求

- Git 分支：`day36-homework-姓名`  
- Commit message 含 `day36`  
- 提交：`作业说明.md` + 截图 + 关键代码 diff  

---

*课后作业 · Day 36*
