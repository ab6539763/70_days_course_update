# Day 36 业务背景 · STAGE PROJECT 2 企业知识库问答（上）

## 1. 事件单

**事件单号**：PROJ2-KB-QA-2026-0801  
**发起人**：产品小陈 + 售前王总  
**严重程度**：P0（STAGE PROJECT 2 · 三天冲刺）  
**答辩时间**：Day 38 周五 14:00  

### 1.1 客户原话

> 「我们要的不是通用聊天机器人，是 **企业知识库问答**：  
> 上传员工手册、退款制度、API 文档，问『怎么退款』要 **一句话答清楚 + 标明引用文件名**；  
> 知识库里没有的内容，必须说 **不知道**，不能瞎编。」

### 1.2 与 Day 24 / Day 33 的关系

```text
  Day 24   Web SSE + 会话持久化（聊天壳子）
  Day 33   Hybrid 检索 + Rerank（检索内核）
  Day 36 ★  ingest + KB + RAG API（后端）
  Day 37     前端引用徽章 + 上传 + 联调
  Day 38     答辩 + Code Review
```

张工签收标准（Day 36）：

1. 多格式文档上传并建索引  
2. Hybrid + Rerank 检索可返回 top-k 引用  
3. `/api/chat` 与 `/api/chat/stream` 均带 `citations`  
4. `verify_project2.py` 全绿  

## 2. 今日任务

```text
  上午  需求评审 + 数据模型 + document_ingest
  下午  hybrid_retriever + rag_service + main.py
  验收  verify_project2（ingest / ask / stream / citations）
```

## 3. 演示脚本（后端 API）

```bash
# 1. 健康检查
curl http://127.0.0.1:8000/health

# 2. 上传文档
curl -F "files=@data/sample_docs/refund_policy.md" \
  http://127.0.0.1:8000/api/documents/upload

# 3. 问答（含引用）
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"退款政策是什么？","session_id":"demo-1","stream":false}'
```

---

*STAGE PROJECT 2 · Day 36 后端交付日*
