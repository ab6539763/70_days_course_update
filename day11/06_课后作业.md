# Day 11 课后作业

> 提交目录：`homework/day11/`  
> Git commit message 建议：`feat(day11): 文件IO与关键词统计作业`

---

## 必做 A · 文件读写（30 分）

### A1. 日志追加器

在 `homework/day11/log_append.py` 中实现：

```python
def append_log(message: str, log_path: str = "output/homework.log") -> None:
    """用 with 以 UTF-8 追加一行，格式：[YYYY-MM-DD HH:MM:SS] message"""
```

`main()` 调用两次，验证文件有两行且中文正常。

### A2. CSV 汇总

读取课程 `data/sample_contacts.csv`，在 `homework/day11/summary.csv` 写出仅含 `department` 与 `count` 两列的汇总（可用 `csv.DictWriter`）。

---

## 必做 B · pathlib 与遍历（25 分）

### B1. 统计目录

在 `homework/day11/dir_stats.py` 实现：

```python
def count_by_suffix(root: Path) -> dict[str, int]:
    """递归统计 root 下各后缀文件数量（小写后缀，含点，如 '.txt'）。"""
```

对 `day11/code/data` 运行并打印结果，写入 `notes.md`。

### B2. 相对路径

解释：`path.relative_to(root)` 与 `str(path)` 在 JSON 报告里为何优先用相对路径？（`notes.md` 3–5 句）

---

## 必做 C · 正则与关键词（25 分）

### C1. 手机号脱敏

在 `homework/day11/mask_phone.py` 用 `re.sub` 将文本中 11 位大陆手机号替换为 `138****8001` 形式（保留前 3 后 4）。

### C2. 扩展统计

复制 `doc_keyword_stats.py` 到作业目录，增加 `--min-hits` 参数：仅将 **至少有一个关键词命中数 ≥ min_hits** 的文档写入报告 `documents`（`summary` 仍统计全部扫描文件）。

---

## 必做 D · 报告验收（20 分）

1. 对 `data/sample_docs` 运行你的 CLI，提交生成的 JSON 报告副本到 `homework/day11/reports/`。  
2. 在 `notes.md` 手工核对任一关键词在 `faq_refund.txt` 中的次数与 JSON 一致（截图或文字说明）。

---

## 选做 E · 关键词 CSV 输入

支持 `--keywords` 指向 `.csv` 文件，读取第一列或名为 `keyword` 的列作为关键词列表。

---

## 选做 F · 并入 sparktech

在 `homework/day11/sparktech_ext/` 增加 `utils/docs.py`：

- `iter_text_files(root: Path)`  
- `read_document(path: Path) -> tuple[str | None, str | None]`  

与 Day 10 包风格一致，并在 `notes.md` 说明如何 `sys.path` 导入。

---

## 挑战 G · 抽样审计

用 `random.sample` 从扫描结果中随机抽 2 个文件，打印路径与 `char_count`，并说明如何用抽样降低人工核对成本（`notes.md`）。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `log_append.py` | A |
| `summary.csv` | A |
| `dir_stats.py` | B |
| `mask_phone.py` | C |
| 扩展版 `doc_keyword_stats.py` | C |
| `reports/*.json` | D |
| `notes.md` | B + D |
| `sparktech_ext/` | F 选做 |

---

**截止时间**：Day 12 上午 09:00 前 push 到个人分支。
