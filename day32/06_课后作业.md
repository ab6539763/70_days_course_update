# Day 32 课后作业

## 必做
1. 跑通三个 demo + verify 全绿  
2. 任选 5 条口语问句，记录改写前后 top-1 是否变化  
3. 解释 HyDE 与 Query Rewrite 的区别（≥100 字）

## 选做
4. 将 `MultiQueryRetriever` 接入 LangChain `EnsembleRetriever` 概念对比  
5. 实现 `LLMChainExtractor` 式逐句压缩（mock 即可）

## 挑战
6. 串联：Rewrite → Multi-Query → HyDE → Compress 一条 CLI
