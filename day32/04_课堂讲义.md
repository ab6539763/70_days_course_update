# Day 32 课堂讲义 · 高级 RAG

## 第一章 · 查询改写（09:00–10:30）

用户问法 ≠ 文档写法。`mock_rewrite` 用正则统一口语：

```python
("退钱|能退吗", "退款"),
("key|密钥", "API Key"),
```

live 模式用 LLM 输出 JSON `{"rewritten": "..."}`。

运行：`python3 query_rewrite_demo.py`

## 第二章 · Multi-Query（10:30–12:00）

思路：一条问句生成多条检索 query，合并结果。

```python
sub_queries = generate_sub_queries("能退钱吗")
# ['能退钱吗', '退款？', '退款政策 到账时间']
```

课堂关注：**去重键**用 `page_content` 前缀，**分数**取 max。

## 第三章 · HyDE（14:00–15:30）

1. LLM 生成「假设答案」  
2. 对假设答案做 embedding  
3. 用该向量检索真实文档  

适用：问句短、答案型知识库。

## 第四章 · 上下文压缩（15:30–16:30）

```python
compress_context(docs, max_chars=600)
```

按检索顺序拼接，超限截断——控制 Day 33 送入 reranker/LLM 的体积。

## 第五章 · 验收

```bash
python3 verify_day32.py
```

---

**预习 Day 33**：BM25 + 向量 Hybrid、RRF 融合、Reranker、Parent Document Retriever。
