# Day 46 课后作业

## 必做

### B1 · 护栏扩展

在 `agent_guardrails.py` 的 `BLOCKED_KEYWORDS` 增加 2 条你们业务敏感词，并补充 `verify_day46` 测试。

### B2 · Trace 标注

运行 `observability_demo.py`，将 JSON 导出保存为 `homework/day46/trace_sample.json`，用箭头标出哪一层是 llm、哪一层是 tool。

### B3 · 验收

`verify_day46.py` 全绿截图。

## 选做

### O1 · LangSmith 实连

配置 `.env` 中 `LANGCHAIN_TRACING_V2=true`，对一个 LangChain chain 开启真实 trace（需 Key）。

### O2 · 成本估算

假设每轮 LLM 2000 input + 500 output token，单价见 Day 15，计算 12 轮死循环费用（写 `homework/day46/cost_estimate.md`）。

## 挑战

### C1 · 将 RetryAgentWrapper 套到 Day 45 Agent

写 `homework/day46/guarded_agent.py`，组合 `IntegratedAgentReview` 与护栏。

---

*答案见 [07_作业参考答案.md](./07_作业参考答案.md)*
