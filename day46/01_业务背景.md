# Day 46 业务背景 · 预发 Agent 死循环与账单告警

## 1. 事件回顾

**事件单号**：OPS-AGENT-LOOP-2026-0746  
**时间**：2026-07-05 23:14  
**现象**：单用户会话 12 轮 `search_knowledge`，token 费用 ¥47，响应 38s  
**根因初判**：无 `max_rounds`、无 tracing、无输入护栏  

### 1.1 失败模式清单（现场白板）

| 模式 | 表现 | 今日代码 |
|------|------|----------|
| 死循环 | 反复调用同一工具 | `max_rounds` + trace 标记 |
| 超时 | 上游 API 无响应 | `RetryAgentWrapper` + 退避 |
| 注入 | 用户篡改 system | `InputGuard` |
| 泄漏 | 输出含手机/身份证 | `OutputGuard` PII |
| 级联故障 | 下游全挂 | `CircuitBreaker` |

### 1.2 干系人诉求

张工：*「Day 46 先把稳定性套件落地，Day 47 Text-to-SQL 必须套护栏。」*

## 2. 今日任务

```text
  上午  失败模式 → 可观测性概念
        │
        ▼
  下午  agent_guardrails + observability_demo
        │
        ▼
  下午  retry_agent_wrapper + verify_day46
```

## 3. 与 LangSmith 的关系

本日 `observability_demo.py` 用内存 Span 模拟 LangSmith Run 树；有 Key 的同学可对照开启 `LANGCHAIN_TRACING_V2=true`（选做）。

---

*需求详见 [02_需求文档.md](./02_需求文档.md)*
