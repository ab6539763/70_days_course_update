# Day 46 作业参考答案

## B1 · 护栏扩展示例

```python
BLOCKED_KEYWORDS = (
    "删除全部数据",
    "drop database",
    "rm -rf /",
    "导出全部用户",  # 新增
    "绕过审批",      # 新增
)
```

## B2 · Trace 标注要点

- `llm_plan` / `llm_summarize` → span_type=llm  
- `tool_lookup_order` → span_type=tool  
- 根节点 `agent_run` → span_type=agent  

## O2 · 成本估算公式

```
cost = rounds × (input_tokens × price_in + output_tokens × price_out)
```

12 轮示例（假设 ¥0.01/1K input，¥0.03/1K output）：

```
12 × (2000×0.01/1000 + 500×0.03/1000) ≈ ¥0.42（教学单价请按实际替换）
```

## C1 · guarded_agent 骨架

```python
from agent_guardrails import AgentGuardrails

g = AgentGuardrails()
def run(q):
    return g.wrap_run(q, lambda text: IntegratedAgentReview().handle_user_input(text))
```

---

*主课：[04_课堂讲义.md](./04_课堂讲义.md)*
