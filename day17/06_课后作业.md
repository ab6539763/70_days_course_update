# Day 17 课后作业

> 提交目录：`homework/day17/`  
> Git commit message：`feat(day17): Prompt 模板库作业`

---

## 必做 A · 四要素模板（25 分）

### A1. 手写 PromptTemplate

在 `homework/day17/my_template.py` 中：

1. 定义一条 **工单优先级判断** 模板（四要素齐全）  
2. 调用 `LLMClient`（mock 即可）并打印 `build_user_prompt()`  

输入样例：`VIP 客户反馈支付失败，影响门店营业。`

### A2. 分隔符检查

在 `notes.md` 说明：为何用户输入必须用 `<<<input>>>` 包裹？举 1 个注入例子。

---

## 必做 B · Shot 策略实验（25 分）

### B1. 扩展 few-shot

复制 `few_shot_demo.py` 到作业目录，新增 **2-shot** 配置，对比输出。

### B2. 实验报告

在 `notes.md` 用表格记录：zero / one / few 三种模式下 mock 输出差异（各 1 行）。

---

## 必做 C · prompt_library 扩展（35 分）

### C1. 新增 2 条 JSON 模板

在 `homework/day17/prompt_library/` 新增：

| 文件 | 任务 | 要求 |
|------|------|------|
| `11_classify_urgency.json` | 紧急度分类 | JSON：urgency, reason |
| `12_summary_email.json` | 邮件摘要 | ≤ 40 字 |

### C2. 批量运行

编写 `run_my_library.py` 加载你的库（含课程 10 条 + 新增 2 条），打印每条 task 与 output。

### C3. 验收

```bash
python3 verify_day17.py   # 课程目录
# 作业目录自建 verify 或截图 run 输出
```

---

## 选做 D · 角色扮演矩阵（15 分）

为同一输入 `「这破软件天天崩」` 写 3 种 role：

- 客服教练（礼貌改写）  
- 产品经理（提取需求）  
- 质检员（违规检测）  

各输出 1 条 Prompt 与 mock 结果。

---

## 选做 E · live 对比

有 API Key 的同学：选 P07 模板，对比 mock vs live 的 JSON 字段完整性，写入 `notes.md`。

---

## 挑战 F · 模板校验器

实现 `validate_template(data: dict) -> list[str]`：

- 缺 `instruction` 报错  
- `classification` 任务缺 `output_format` 警告  
- `examples` 非 list 报错  

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `my_template.py` | A |
| `notes.md` | A,B |
| `prompt_library/11_*.json` | C |
| `prompt_library/12_*.json` | C |
| `run_my_library.py` | C |

**截止**：Day 18 上午 09:00 前 push。
