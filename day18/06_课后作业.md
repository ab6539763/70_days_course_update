# Day 18 课后作业

> 提交：`homework/day18/`  
> Commit：`feat(day18): 意图分类与安全作业`

---

## 必做 A · 意图分类器扩展（35 分）

### A1. 新增意图样例

在 `homework/day18/data/my_queries.txt` 新增 **10 条** 标注消息（含至少 2 条易混淆：咨询 vs 技术支持）。

### A2. 扩展 classify

子类 `MyIntentClassifier`，对 `confidence < 0.6` 强制 `need_human=true`。

### A3. 运行报告

`run_classifier_report.py` 输出 markdown 表格：query | intent | confidence | route。

---

## 必做 B · 注入防御（25 分）

### B1. 新增 3 条攻击样例

写入 `injection_cases.json`，含：Unicode 混淆、多语言混合、假 XML 闭合标签。

### B2. 规则扩展

在作业目录复制 `sanitize_user_input`，至少新增 **2 条** 正则规则。

### B3. notes.md

记录：规则层 vs LLM 层各拦截了几条？有无误杀？

---

## 必做 C · 结构化输出（25 分）

### C1. 工单 JSON

用 JSON Mode 生成工单对象：`title`, `priority`, `assignee`, `tags`（列表）。

### C2. Function Calling

新增工具 `create_ticket(title, priority)` mock 实现，演示一次完整 tool_call。

---

## 选做 D · CoT 实验

选一道多步应用题，live 模式对比 direct vs CoT 输出（截图 + 200 字分析）。

---

## 选做 E · Self-Consistency

有 Key 的同学：同一 CoT Prompt 采样 5 次，手写投票逻辑，对比单次 CoT。

---

## 挑战 F · 意图混淆矩阵

用 8 条样例构建 7×7 混淆矩阵骨架（预期 vs 实际），为 Day 19 评估预习。

---

## 提交清单

| 文件 | 必做 |
|------|------|
| `data/my_queries.txt` | A |
| `my_intent_classifier.py` | A |
| `run_classifier_report.py` | A |
| `injection_cases.json` | B |
| `sanitize_extended.py` | B |
| `notes.md` | B |
| `ticket_json_demo.py` | C |

**截止**：Day 19 上午 09:00。
