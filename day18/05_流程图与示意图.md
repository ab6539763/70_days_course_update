# Day 18 流程图与示意图

## 1. 意图分类端到端

```mermaid
flowchart TB
    U[用户消息] --> S{规则注入检测}
    S -->|blocked| R1[拒绝回复]
    S -->|pass| IC[IntentClassifier]
    IC --> LLM[LLM + JSON Mode]
    LLM --> P[extract_json_block]
    P --> V{schema 校验}
    V -->|fail| FB[bot_fallback]
    V -->|ok| RT[route_by_intent]
    RT --> B1[bot_*]
    RT --> H[human_escalation]
```

## 2. CoT vs Direct

```ascii
Direct:  问题 ──────────────────────▶ 答案

CoT:     问题 ──▶ 步骤1 ──▶ 步骤2 ──▶ 步骤3 ──▶ 答案
                      └──── Self-Consistency 多次投票 ────┘
```

## 3. Self-Consistency 投票

```mermaid
flowchart LR
    P[同一 Prompt] --> S1[采样1 → 15]
    P --> S2[采样2 → 15]
    P --> S3[采样3 → 14]
    S1 --> V[投票]
    S2 --> V
    S3 --> V
    V --> A[最终答案: 15]
```

## 4. Tree of Thoughts 分支

```text
                    用户投诉物流延迟
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
    道歉+补偿券        解释天气原因        转人工专员
    score=0.82         score=0.45         score=0.91 ★
```

## 5. 注入防御双层

```mermaid
sequenceDiagram
    participant User
    participant Rule as sanitize_user_input
    participant LLM as hardened LLM
    participant App

    User->>Rule: 原始输入
    alt 命中注入模式
        Rule-->>App: blocked=true
    else 通过
        Rule->>LLM: <user_input> 包裹
        LLM-->>App: JSON safe_response
    end
```

## 6. Function Calling 流程

```text
User: 查订单 ORD-8821
         │
         ▼
    Chat API (tools=[query_order_status])
         │
         ▼
    tool_call: {order_id: "ORD-8821"}
         │
         ▼
    本地 query_order_status()
         │
         ▼
    Assistant: 您的订单已发货…
```

## 7. 课程衔接

```mermaid
flowchart LR
    D17[Day17 JSON 样例] --> D18[Day18 JSON Mode]
    D18 --> D19[Day19 评估指标]
    D18 --> D36[Day36 RAG 路由]
    D18 --> D48[Day48 Agent 工具]
```

---

*示意图可用于安全评审 PPT*
